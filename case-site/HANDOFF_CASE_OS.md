# CASE OS — Handoff (передача проекта в новую сессию)

_Обновлено: 2026-07-25 · текущая версия платформы **v4.49.2**_

## 1. Где лежат все файлы и данные

- **Репозиторий:** `caseadvisoryservicess/case-site` (GitHub).
- **Рабочая ветка со ВСЕМ актуальным кодом:** `claude/new-session-2kq1ff`.
  - С v4.36.0 `main` синхронизируется с рабочей веткой (fast-forward по явному разрешению
    пользователя) — платформа больше не живёт в одной-единственной ветке. Разработка — по-прежнему
    только в рабочей ветке; в `main` пушить лишь по явному разрешению.
- Как забрать всё в новой сессии:
  ```
  git fetch origin claude/new-session-2kq1ff
  git checkout claude/new-session-2kq1ff
  ```
- История: v4.31.20 → влит пользовательский hosting-ready билд **v4.32.0–v4.32.7**
  (данные-грид, sticky-шапки, AGX lockdown, `unit_patch.php`/`units_batch.php`) → **v4.33.0** (P0-1).
  Архив документации прошлых релизов — `docs/archive/`.

## 2. Что это за проект

CASE OS — операционная система для агентства недвижимости (аренда/продажи/advisory/гео-аналитика).
Архитектура:
- **Фронтенд:** `os/index.html` (56 КБ: разметка, стили, APP_VERSION) + **`os/core.js`** (ядро: все основные экраны, вынесено из index.html в v4.49.0) + отложенные JS-модули `os/v*.js` (например `v35-stable.js` = База брендов, `v32-upgrade.js` = SCR-продажи, `v410-feature-flags.js` = управление модулями, `v417-master-plan.js` = мастер-планировки, `v420-geo-*` = гео).
- **Бэкенд:** PHP в `os/api/*.php` + MySQL. Ключевые: `lib.php` (ядро, права, таблицы), `state.php` (общий blob состояния), `users.php`, `data.php` (generic CRUD).
- **PWA:** `os/sw.js` (network-first, кэш `case-os-vNNNNN`).
- Режимы: **backend** (есть `os/api/config.php` + MySQL) или **demo** (только `?demo=1`, данные в localStorage).

## 3. Как это развёрнуто (ВАЖНО — источник всех «ничего не меняется»)

- Живой сайт: **`https://caseadvisory.uz/os/`** на хостинге **DirectAdmin** (`caseadvisory.uz:2222`, логин `wwwcasea`). Домен `.com` — другой сервер, туда файлы класть бесполезно.
- **Автодеплоя из GitHub нет** (пользователь не хочет зависеть от GitHub). Обновление = вручную загрузить ZIP в `domains/caseadvisory.uz/public_html/` через Менеджер файлов → Extract с перезаписью `os/`.
- Поэтому: коммит/ZIP сами по себе НЕ меняют живой сайт. Обязательно проверять номер версии вверху страницы (`v4.31.xx`) после загрузки.
- Каждый релиз собирать как **`CASE_OS_vX_os_only.zip`** (только папка `os/`, БЕЗ `config.php` — чтобы не затирать доступ к БД и не трогать лендинг).

## 4. Правила работы (обязательно соблюдать)

- Разработка только в ветке `claude/new-session-2kq1ff`. **Не пушить в main без явного разрешения. PR не создавать без явной просьбы.**
- ZIP всегда **исключает секреты**: `config.php`, `api/config.php`, `*.sqlite`, `*.sqlite3`, `*_brands_test.xlsx`, `sql/backups/*`.
- **FULL-ZIP дополнительно исключает dev-обвязку** (иначе уезжает на боевой хостинг): `-x '.claude/*' -x 'CLAUDE.md' -x 'graphify-out/*'`. Проверено: без этих ключей в архив попадали 46 файлов графа + конфиг хуков. `.gitignore` архив НЕ спасает — zip его не читает. (`os_only` не задет: он собирается только из папки `os`.)
- Идентификатор модели не писать в коммиты/PR/код (только в чате).
- Область репозиториев ограничена `caseadvisoryservicess/case-site`.
- Каждый релиз: поднять `APP_VERSION` в `os/index.html` + кэш в `os/sw.js`, прогнать E2E, собрать `_os_only.zip`, запушить, отдать пользователю файл.
- **С v4.49.0 ядро живёт в `os/core.js`.** `?v=` у core.js и его запись в `CASE_EXPECTED_MODULES` поднимать ТОЛЬКО при реальной правке ядра — иначе теряется весь смысл выноса (браузер снова качает 950 КБ каждый релиз). APP_VERSION намеренно остался в index.html.

## 5. Тестирование

- Playwright (chromium): `/opt/pw-browsers/chromium-1194/chrome-linux/chrome --no-sandbox`.
- Тесты теперь **в репозитории**: `docs/qa/tools/verify_full_qa.js` (браузерный QA, 35 проверок; статик-сервер отдаёт 404 на `/api/*` — иначе `apiGET` при 200+не-JSON считает, что BACKEND есть) и `docs/qa/tools/p01_vm_test.js` (VM-тест ядра снапшотов, 18 проверок). Запуск: `npm i playwright-core` в scratchpad, затем `node verify_full_qa.js /path/to/os`. Результаты релизов — `docs/qa/*.json`.
- Известное ограничение среды: прокси **блокирует внешние хосты** (`caseadvisory.uz` → 403) и `localhost`-навигацию в части сред. Живой сайт из среды не открыть — проверять по скриншотам пользователя. PHP есть, но **без MySQL-драйвера** — серверную логику проверять `php -l` + логическими VM-тестами, реальную БД тестирует пользователь на staging.

## 6. Что уже сделано (последние релизы)

- **v4.50.6: границы районов Ташкента положены локально (из архива пользователя):**
  - **НАХОДКА:** слой районов города брался ИЗ ИНТЕРНЕТА. `ensureGeo()` (geoanalytics-studio.html ~1603) пробует по очереди `data/tashkent_districts.geojson` → jsdelivr → raw.githubusercontent. Локального файла в пакете НЕ БЫЛО, при недоступности GitHub слой молча пропускался (только `console.info`). Место под файл в коде было предусмотрено, файла не было;
  - собран `os/data/tashkent_districts.geojson` из KML внутри архива: 12 районов, 13 полигонов, 12 177 точек, 262 КБ, WGS84 (перепроецирование не нужно). Площадь 438 км², bbox 41.16–41.42 / 69.12–69.47 — сходится;
  - **ГЛАВНАЯ ТОНКОСТЬ:** `nn()` (studio ~1559) вычищает `[^a-zа-я']`, поэтому узбекские «ҳ» (U+04B3) и «ё» (U+0451) ВЫПАДАЮТ: «Янгиҳаёт» → «янгиат», в `SYN` такого нет. Поэтому в каждый feature положено ЛАТИНСКОЕ имя ровно в написании ключей SYN (`Chilonzor`, `Yangihayot`, `Shayxontohur`, `Mirzo Ulugbek`…). `matchD()` перебирает ВСЕ строковые свойства, так что узбекское и русское имена рядом не мешают;
  - GDAL/pyshp/fiona в песочнице нет — конвертировал из KML штатным `xml.etree`, .shp не трогал;
  - файл добавлен в ASSETS sw.js: студия кэшировалась, а слой без сети всё равно был пуст;
  - **ГРАБЛИ В ТЕСТЕ:** `DISTGEO`/`gDist` объявлены через `let` → в `window` НЕ попадают, обращаться надо по имени напрямую в `page.evaluate`. И данные приходят РАНЬШЕ отрисовки — ждать надо не `DISTGEO`, а непустой `gDist.getLayers()`, иначе ложное падение;
  - тест `v4506_districts_local.js` (5/5) ОБРЫВАЕТ весь внешний трафик. Проверено, что ловит: без файла 0 районов и 5 обращений к GitHub вместо 2;
  - **ОСТАЛОСЬ ИЗ ИНТЕРНЕТА:** `REGGEO` — границы Ташкентской ОБЛАСТИ (22 территории), локального файла нет, те же 2 обращения к GitHub. Нужен такой же архив по области;
  - автономная версия (`docs/standalone/`) использует ту же цепочку — для офлайна ей нужна папка `data` рядом с HTML;

- **v4.50.5: «⚠ НЕ сохранено: USERS, Бенчмарки, ROLES, AUDIT, Геоаналитика, CASE_PARTNERS» при каждом сохранении:**
  - **ПРАВКИ СОХРАНЯЛИСЬ.** `state.php` пишет ПО-КЛЮЧЕВО: разрешённые роли ключи сохраняет, закрытые восстанавливает из `$oldData` и возвращает в `rejected_keys` (state.php:379-392). Тест это фиксирует: проверка «правка доехала» проходит и ДО фикса, и после. Сотрудник видел тревожный тост и делал вывод, что не сохраняется ничего;
  - причина: GET вырезает недоступные роли ключи (`role_visible_state_keys`), клиент их не получает → `_syncedKeyJson[k]===undefined`. Но у клиента для USERS/ROLES/AUDIT/BENCH/GEO_DATA есть ВСТРОЕННЫЕ непустые значения по умолчанию, поэтому фильтр v4.50.0 (`_stripUnsyncedEmpty`) их НЕ отсекал — он отбрасывает только `[]`/`{}`. Они уходили в POST, отличались от хранимого → `rejected_keys`;
  - **почему нельзя было просто «не слать всё, чего сервер не присылал»:** так терялось бы ПЕРВОЕ сохранение раздела, который роль писать вправе, но в базе его ещё нет. Нужен внешний признак — поэтому источником истины сделан сервер;
  - ФИКС: `state.php` GET отдаёт `restricted_keys`, вычисляя их ТЕМ ЖЕ выражением, что ветка отказа в POST (`array_diff(all_shared_state_keys(), $allowed, [ROLE_WORKSPACES,USER_WORKSPACES,MODULE_FLAGS])`) — иначе списки разойдутся. Клиент: `_restrictedKeys` + `_isRestricted()`, фильтр в ДВУХ местах — `_stripUnsyncedEmpty` и дельта-сборка (core.js ~181). Подхват в ТРЁХ местах чтения state.php (загрузка, фоновой опрос, слияние при конфликте);
  - старый сервер без `restricted_keys` → `_restrictedKeys=null` → поведение прежнее; проверяется тестом (обновление клиента раньше сервера ничего не ломает);
  - **МОК ПРИШЛОСЬ ДОУЧИТЬ:** он не отдавал `restricted_keys` и не отклонял закрытые ключи при POST — без этого тест зелёный на сломанном коде. Проверил обратное: со снятым фильтром тест падает и показывает ровно те 6 разделов из жалобы;
  - **ОТКРЫТО, РЕШЕНИЕ ВЛАДЕЛЬЦА:** `BENCH` (область `bench`, state.php:127) закрыт для роли жалобщика. Если правили именно бенчмарки — эта правка ДЕЙСТВИТЕЛЬНО не сохранялась, тост был по делу. Та же развилка, что с ЛСР в v4.50.1: UI разрешает, сервер нет. Надо спросить, какую таблицу правили;

- **v4.50.4: «нет мобильных операторов» — ТРИ бага от одной причины (CATTYPES/CATALCO на старых именах категорий):**
  - **«Оператор связи» БЫЛ в коде** — `CATTYPES['Услуги, киоски, спец. магазин']` (core.js:528). Недостижим по трём причинам разом;
  - **(1) `const CATTYPES` НЕ попадает в `window`** — top-level `const` в обычном скрипте не создаёт свойство window, а v35-stable:344 читал `window.CATTYPES` → всегда undefined → поле «Тип» пустое для ВСЕХ 16 категорий. Проверено прогоном: типов 0 везде;
  - **(2) все 13 ключей CATTYPES — имена ДО таксономии v3.5.7**, ни один не равен каноническому («Места общественного питания» ≠ «F&B / рестораны и кафе»). Даже с доступом лукап не нашёл бы ничего;
  - **(3) `CATALCO` (core.js:541) по той же причине** → `catHasAlcohol('F&B / рестораны и кафе')` = false, галочка алкоголя не появлялась там, где нужна;
  - ФИКС: явная таблица `CATTYPES_TO_CANON` (13 строк, глазами проверяемая) → `CANON_CATTYPES` + `ALL_CATTYPES` + `CANON_CATALCO`, выставлены в window. **НЕ через catNorm/aliasHit** — ключ «Услуги, киоски, спец. магазин» не совпадает с псевдонимом «Киоски, спец. магазины», нечёткое сравнение тут ненадёжно. Старый `window.CATTYPES` сохранён: `typesDatalist`/core.js:546 работают со старыми ключами;
  - v35-stable: поиск типов и алкоголя через `catNorm(cat.value)` — иначе псевдоним «Услуги» не находил ничего; пустая категория → `ALL_CATTYPES` (84);
  - «Telecom» в подкатегориях → «Оператор связи / салон связи», старое имя + 6 написаний в SUB_ALIAS_GROUPS (записи не теряются);
  - **МОИ ЛОЖНЫЕ ГИПОТЕЗЫ по дороге, не повторять:** (а) думал, что список подкатегорий пуст — НЕТ, 32 варианта, проверено в браузере; пустым было поле «Тип»; (б) думал, что строки 69 и 87 — два конкурирующих списка подкатегорий; НЕТ, 69 это `CAT_ALIAS_GROUPS`, 87 — `CANON_SUBS`; (в) `openBrand(id)` открывает ПРОСМОТР существующего бренда и падает на `b.hist` при null — форма создания это `window.brandEdit('')`;
  - таксономия в остальном чистая: 16 категорий, 286 подкатегорий, пустых списков нет, дубликатов внутри списка нет. Пересечения: «Аксессуары» (мода/ювелирные) и «Книги и канцтовары» (дети/спец.ритейл) — осмысленные, внутри своей категории subNorm разрешает верно, оставил;
  - ОТКРЫТО: типов нет у «Специализированный ритейл», «Офис / коворкинг», «Склад / технические помещения», «Прочее» — их не было и в исходных данных;
  - тест `docs/qa/tools/v4504_brand_taxonomy.js` 13/13; набор 133/133; core.js изменён → версии подняты до 4.50.4, у `v35-stable.js` поднят `?v=` (в CASE_EXPECTED_MODULES его нет);

- **v4.50.3: закрыты находки внешнего аудита пакета v4.50.2 (проверял каждую по коду — все реальные):**
  - **ДАМПЫ БАЗЫ ЛЕЖАЛИ В ВЕБ-КОРНЕ.** `api/backup.php` писал в `os/sql/backups`, имена предсказуемы до секунды. Запрет `.sql.gz` был ТОЛЬКО в `api/.htaccess` и на эту папку не действовал, `Options -Indexes` прячет листинг, но не запрещает скачивание по имени. Заодно отдавался `sql/install_all.sql`. Теперь: `realpath(__DIR__.'/../../..').'/caseos_backups'` (выше public_html) + одноразовый `rename` старых копий + `caseos_backup_dirs()`/`caseos_find_backup()` ищут в ОБЕИХ папках, чтобы ничего не пропало при нехватке прав. Вторым рубежом FilesMatch на `.sql|.sql.gz|.sqlite|.log|.env` в корневом `.htaccess` и `Require all denied` в `sql/` и `sql/backups/`;
  - **ГРАБЛИ ПРИ СБОРКЕ:** архив и манифест раньше исключали `./sql/backups/*` целиком — новые `.htaccess` выпали бы из пакета. Исключать надо только `*.sql.gz`. Файлов в манифесте стало 148 вместо 144;
  - **CSP ЗАПРЕЩАЛА ТО, ЧТО САМА СТРАНИЦА ГРУЗИТ:** `script-src 'self'` против PptxGenJS с jsDelivr → выгрузка PPTX падала на боевом Apache; Google Fonts тоже резались. Добавлены ровно три источника. Локально библиотеку не собрать — из песочницы нет доступа к jsdelivr (proxy 403), это записано в CHANGELOG как открытая задача;
  - **BACKEND=true СТАВИЛСЯ БЕЗУСЛОВНО:** `apiGET` делает `try{j=await r.json()}catch(e){}` и возвращает `null`, поэтому статичный сервер, отдающий ИСХОДНИК auth.php с кодом 200, включал серверный вход. Теперь требуется `typeof j.auth==='boolean'` (auth.php отдаёт его во всех ветках — проверено). Тест `v4503_backend_guard.js` 4/4 проверяет ОБА направления, иначе легко сломать обычный вход;
  - sw.js: `quiz-data.js` не было в ASSETS; плюс `addAll` — всё-или-ничего, один битый файл ронял всю предзагрузку молча. Теперь пофайлово + `console.warn` со списком;
  - `zarafshan-l2.svg`: 21 сущность `&nbsp;` (в XML не объявлена) → `&#160;`, проверено `ET.parse`;
  - `.ph-help` 24px → область нажатия 40×40 через `::after` в `@media (pointer:coarse)`, визуальный размер не тронут;
  - **НЕ СДЕЛАНО, ЖДЁТ РЕШЕНИЯ ВЛАДЕЛЬЦА:** геоаналитика в демо-режиме показывает отказ — `v420-geo-studio.js:755` всегда идёт в настоящие `auth.php`+`workspace_access.php`. Это НЕ баг, а защита из v4.32.1 (прямой URL пропускал любую внутреннюю роль). Ослаблять самовольно нельзя — вопрос доступа к данным. Варианты в CHANGELOG: подписанная метка родительской сессии во фрейм либо явная демо-ветка только для чтения;
  - core.js изменён → `?v=` и `CASE_EXPECTED_MODULES['core']` подняты до 4.50.3 (в v4.50.1/2 ядро не трогали и версия намеренно оставалась 4.50.0);

- **Автономная геоаналитика: добавлен проект Samsung BC (41.34874282632705, 69.28821941229232):**
  - пользователь прислал файл BOGISHAMOL_PARK (отчёт по парку в Учтепе) со словами «в приложенном html только ботаника» — но в ТОМ файле ни Ботаники, ни проектов нет вовсе; Botanica/Taxtapul живут в `docs/standalone/CASE_OS_Geo_Analytics.html`, туда и добавлено. Точка от парка в 11 км — это разные объекты;
  - точка проверена по `os/data/tashkent_districts.geojson` (point-in-polygon): район **Yunusabad**; до Botanica BC 2.37 км, до Taxtapul BC 2.34 км;
  - добавлено в ТРЁХ местах: `PROJECTS.samsung`, `<option value="samsung">` в #proj, запись в массив BC — **строго В КОНЕЦ** (151-я), потому что правки карточек в localStorage привязаны к НОМЕРУ записи; вставка в середину молча перевесила бы их на чужие БЦ. Статус записи «Planned», comment «Планируется строительство»;
  - тест `samsung_test.js` (10/10, в scratchpad): три пункта в списке, координаты точные, прежние правки (метки на BC[0]/BC[7]) не съехали, переключение перецентрирует карту;
  - ВАЖНО для следующего раза: в версии ОС (`os/geoanalytics-studio.html`) справочник другой — там `PROJECTS={project:{...}}` с произвольным проектом, туда Samsung НЕ добавлял (не просили и не нужно);

- **Автономная геоаналитика: перенесены подложки карт из ОС (9 штук, переключатель справа сверху):**
  - пользователь прислал свою доработку поверх моей версии (+`renderStaticMap` — карта в SVG, когда Leaflet недоступен; массив BC и 7 секций не тронул, проверено);
  - перенёс из `os/geoanalytics-studio.html` блок подложек целиком: класс `YandexTiles` (у Яндекса эллипсоидальная сетка 3395, обычный tileLayer даёт смещение), `window.BASES` из 9 слоёв, `L.control.layers(...,{position:'topright',collapsed:true})` и автозамену подложки при недоступных тайлах;
  - **ГРАБЛИ ПРИ ИЗВЛЕЧЕНИИ БЛОКА:** он заканчивается `watch(cur);})();})();` — последний `})();` закрывает ВНЕШНЮЮ обёртку файла ОС, брать его нельзя. Резать строго по `watch(cur);})();`, потом проверять баланс скобок (у меня 0/0);
  - **ГРАБЛИ ПРИ ТЕСТИРОВАНИИ:** в песочнице нет сети → Leaflet с CDN не грузится → `HASMAP=false` → весь блок карты пропускается, и тест по подложкам молча «проходит», ничего не проверив. Тест `geo_standalone_maps.js` подставляет Leaflet локально через `page.route` (вынимая его из `os/geoanalytics-studio.html`, где он встроен) и отдаёт 1×1 PNG вместо тайлов. 8/8;
  - **ВАЖНО — ВЕРСИЯ В ОС ВПЕРЕДИ, НЕ ЗАМЕНЯТЬ ЕЁ АВТОНОМНОЙ:** `os/geoanalytics-studio.html` 2138 строк против 1285. В ОС есть, а в автономной нет: встроенный Leaflet, markercluster (7 мест), `asaas_saved_probes`, гейт прав (`geo-auth-pending`), произвольный проект (`PROJECTS={project:...}`) вместо двух зашитых botanica/taxtapul. Шапки слоёв в ОС УЖЕ чистые — правка подписей туда не нужна (8 из 9 моих шаблонов там не находятся, потому что разметка давно другая). Обмен идёт из ОС в автономную, не наоборот;
  - **РАЗНЫЕ СПИСКИ БЦ — мина:** автономная 150 (первые два Botanica BC и Taxtapul BC), ОС 148 тех же без этих двух, т.е. новый = [2 проекта] + старый, все 148 номеров сдвинуты на +2. Ключ хранения `caseos_bc_edits` У ОБОИХ ОДИН и адресуется по номеру. Спасает только то, что origin разные (ОС на сайте, автономная локально). Если класть автономную на сайт — сперва развести ключи или перейти на устойчивый ключ; учесть, что названия НЕ уникальны (дубль «Infinity, компания консалтинга…»), поэтому ключ по названию нужен с координатами;

- **Автономная геоаналитика (`docs/standalone/CASE_OS_Geo_Analytics.html`) — настройки приведены к виду ОС:**
  - это ОТДЕЛЬНЫЙ самодостаточный файл, в пакет `/os` не входит, сервера не имеет. Раньше в репозитории его не было — пользователь присылал файлом. Теперь лежит в `docs/standalone/` вместе с README;
  - применён тот же приём, что и v4.42.2 в гео-студии: в шапке слоя остаётся только включатель+название, ВСЕ контролы уходят в подписанные `.styrow sub`. Механизм ⚙ (`buildPanel`) в файле уже был точно такой же — он собирает все `.styrow.sub` после строки слоя в `.lopts` и прячет под шестерёнку. Голые input в шапке = то, что осталось в главной `.styrow`;
  - перенесено 9 слоёв / 12 контролов: bcNa+bcLab, mLab, rByLine, medC+medR, phC, dLab, densC+densO, heatC+heatO, плюс подписан ringWc в «Зоне охвата». У медицины из-за контролов в шапке название ломалось на 3 строки — теперь в одну;
  - **ГЛАВНЫЙ РИСК ПРИ ЛЮБОЙ ПРАВКЕ ЭТОГО ФАЙЛА:** правки БЦ хранятся в `caseos_bc_edits` **по индексу массива `BC`** (`BC.forEach((b,i)=>b.id=i)`), а свёрнутость секций в `caseos_panel` — **по индексу `.sect`**. Сдвиг массива/секций не «теряет» данные, а МОЛЧА ПЕРЕВЕШИВАЕТ их на чужие БЦ. Поэтому массив BC оставлен байт-в-байт (проверяется в тесте), число секций 7 не менялось;
  - обработчики везде навешаны по `$(id)`, позиционных зависимостей нет (единственный `parentNode` — в Leaflet-контроле), поэтому перенос по DOM безопасен;
  - тест `docs/qa/tools/geo_standalone_settings.js` (11/11): кладёт «старые» правки в localStorage, грузит файл, проверяет что правки на своих БЦ, состояние секций восстановлено, в шапках нет голых контролов, у всех перенесённых есть `label[for]`, ⚙ раскрывает панель, изменение контрола не роняет отрисовку;
  - Leaflet тянется с CDN — без сети карта не строится (`HASMAP` это гасит), панель работает;

- **v4.50.2: мини-тест перестал выдавать правильный ответ:**
  - в `os/quiz-data.js` (1000 вопросов × ru/uz/en) к вариантам были дописаны пять приписок на язык. Русский: «в рамках процедуры», «вариант процесса», «операционный вариант», «рабочий вариант», «для проверки». Узбекский/английский — свои пять;
  - **приписки стояли ТОЛЬКО у неверных ответов — 0 из 1000 у верного**. Вариант без приписки всегда правильный: 470 вопросов выдавали ответ сразу, ещё 294 отсекали часть вариантов. Заголовок файла утверждал «сбалансированы по длине» — на деле удлинялись только дистракторы;
  - убрано во всех трёх языках (2411 языковых блоков, ~4000 вставок), дублей ответов не возникло. Проверено, что все вхождения шли после « — » — иначе глобальная замена срезала бы осмысленный текст;
  - **ГРАБЛИ:** приписки бывают ЦЕПОЧКОЙ («X — рабочий вариант — вариант процесса — для проверки —»), поэтому снимать надо в цикле до стабилизации, и очистку концевого тире тоже в цикле — хвосты вида «— —» есть и в исходных данных (17 шт.). У меня сначала концевое тире снималось однократно → осталось 57 битых вариантов;
  - `quiz-data.js` подключался БЕЗ `?v=` — исправление не дошло бы до сотрудников из-за кэша. Версия добавлена;
  - **ОТКРЫТО:** после снятия приписок вскрылась подсказка, которую они маскировали — верный ответ самый длинный в 66% вопросов (норма ~33%), в среднем на 15–17 символов. Механически не чинится, нужно переписывать дистракторы (контент, 1000×3). Позиция верного ответа при этом ровная: A/B/C = 337/338/325;
  - детектор `docs/qa/tools/quiz_tells.js` — приписки, длина, позиция, мусор. СЕЙЧАС ЧЕСТНО ПРОВАЛИВАЕТ проверку длины: это открытый пункт, а не ошибка инструмента. Не «чинить» подгонкой порога;

- **v4.50.1: отклонённая правка помещения больше не выглядит сохранённой:**
  - **КОРЕНЬ жалобы «у Асада (BRJ) данные пропадают»** — это НЕ фильтр из v4.50.0. `os/api/unit_patch.php:7` жёстко закрывает LCR для ролей `AGX`, `BSH`, `BRJ` (403), а в интерфейсе у BRJ `edit:true` (`ROLES.BRJ`, core.js:292) — поля редактируются. Вилка: UI разрешает, сервер отклоняет;
  - при 403 значение ОСТАВАЛОСЬ в памяти и на экране (`rec.cancelled=true;rec.changes={}` очищало очередь, но не откатывало `u`), плюс `persist()`/`cacheLocalCore()` уже записали его в localStorage. Сотрудник видел «сохранилось», жал F5 — значение исчезало. Тост был, но легко пропускается;
  - ФИКС: `unitPatch(id,delta,immediate,prevSrc)` запоминает значение ДО правки в `rec.prev`; в ветке 403 поля откатываются, вызывается `cacheLocalCore()` и перерисовка реестра. 401 и обрыв связи НЕ откатываются — там правка законно ждёт повтора;
  - **ГРАБЛИ:** `unitPatch` вызывается из ДВУХ мест — `wrapOne` (правка ячейки, строка ~152) и `wrapBulk` (~157). Я сначала передал `prevSrc` только в `wrapBulk`, тест показал «значение осталось 999». Патчить надо оба;
  - ВАЖНО: все правки помещений (и карточка `unitSave`, и ячейки) идут через `unit_patch.php`, а не через общее сохранение — `v4327-patch.js` их перехватывает и делает `stopWholeStateSave()`. Поэтому `state_key_denied` по `U` к правкам реестра отношения НЕ имеет;
  - тест `v4501_reject_revert.js` (5/5, мок умеет `failMode:'reject-units'` → 403); набор 133/133;
  - `core.js` НЕ менялся — `?v=` и `CASE_EXPECTED_MODULES['core']` остались 4.50.0, ядро не выпадает из кэша. Поднято только `v4327-patch` → 4.50.1;
  - **ОТКРЫТЫЙ ВОПРОС ВЛАДЕЛЬЦА:** должен ли BRJ править реестр. Если да — снять роль из списка в `unit_patch.php:7` и добавить область `registry`. Если нет — убрать у него кнопки правки в UI. Не решено;

- **v4.50.0: сохранение больше не создаёт ложных отказов (по итогам диагностики боевого сервера):**
  - **КОРЕНЬ:** сервер вырезает из ответа GET разделы, закрытые для роли (`state.php:317`, `role_visible_state_keys`). Клиент их НИКОГДА не получал, держал пустыми — и слал эту пустоту при каждом сохранении. Сервер писал `state_key_denied` и откатывал значение (`state.php:386`, `$incoming[$k]=$oldData[$k]`, ответ при этом `ok:true`). На боевом за неделю: 400 отказов, AUDIT 397 / USERS 396 / ROLES 396 / U 167. Настоящие отказы за этим было не разглядеть;
  - **ВАЖНО, я сначала ошибся:** 167 отказов по `U` — это НЕ потерянные правки реестра, а именно пустой массив от роли без области `registry`. Не повторять эту ошибку при чтении журнала;
  - ФИКС: `_stripUnsyncedEmpty` — пустой раздел, которого сервер не присылал, не отправляется. Стоит в ТРЁХ местах, и это не дубль: (1) `apiPOST` — чокпойнт для всех 12 мест с `{data:stateBlob()}`; (2) отбор изменённых ключей в `_doSaveState` — иначе успешное сохранение переносит весь `pend` в `_syncedKeyJson` и помечает как «сервер это знает» то, чего он не получал, и на следующем сохранении фильтр уже пропустит; (3) `flushState` — дозаливка при уходе идёт через `sendBeacon` мимо `apiPOST`;
  - **ГРАБЛИ:** `let _syncedKeyJson` объявлен ПЕРЕД `apiPOST` намеренно. Был ниже по файлу — самое первое сохранение успевало сработать в TDZ, обращение бросало ReferenceError, `catch` его глушил, и состояние уходило нефильтрованным. Симптом: фильтр работает при прямом вызове, но первый POST всё равно полный;
  - замер: на сервер уезжало 64 раздела (41 пустой) → стало 24. Удаление последней записи НЕ сломано (раздел уже присылали → `_syncedKeyJson[k]` определён → пустое значение уходит как обычно) — проверяется тестом;
  - предупреждение об отказе было `Set` навсегда (один раз за сессию на раздел) → стало `Map` с временем, повтор через 10 мин;
  - **ПРОВЕРЕНО И НЕ ПОДТВЕРДИЛОСЬ:** путь сохранения ЛСР исправен — правка бюджета уходит `unit_patch`, сервер пишет, переживает перезагрузку (`lcr_persist.js`). Отсутствие ставок на боевом — поля просто не заполняли (набор полей соответствует импорту планировки);
  - **ЛОВУШКА В ТЕСТАХ:** таблица ЛСР — вид `go('registry')`, а НЕ `go('reg')` (тот рисует «План/бюджет» с планировками, строк `tr[data-uid]` там нет). Поля ввода появляются только после `toggleRegEdit()`;
  - **ЛОВУШКА В МОКЕ:** `mock_backend.js` принимал `unit_patch` и подтверждал, но не применял к `appState`, тогда как настоящий `unit_patch.php` делает `UPDATE app_state`. Давал ложное «до сервера не доехало» на исправном коде. Исправлено;
  - тесты: `v4500_denied_noise.js` (падает на v4.49.2 — проверено), `lcr_persist.js`; весь набор 133/133;
  - остаточное: `CASE_PORTFOLIO_PROJECTS` уезжает пустым из-за отдельной гонки в демо-данных (модуль засевает 95 записей, они обнуляются между фильтром и отправкой). К этой защите отношения не имеет, на боевом раздел непустой (95 записей);
  - **ДИАГНОСТИКА БОЕВОГО СЕРВЕРА** (`docs/qa/DIAGNOSTIKA_RU.md`): `server_check.php` + `deep_check.php` — запуск в терминале хостинга, только чтение, секреты не печатают, запуск через браузер заблокирован (`PHP_SAPI!=='cli'` → 403). `browser_check.js` — вставляется в консоль. ГРАБЛИ: `printf('%-20s')` в PHP считает БАЙТЫ (кириллица разъезжает) — есть `p()`; `elementFromPoint` вне видимой области ВСЕГДА возвращает null (давал ложное «шапка не отрисована» на таблицах за пределами экрана); права роли лежат в `ROLES[S.role]`, `S.role` — строка; `CASE_EXPECTED_MODULES` — `const` в index.html, в `window` её НЕТ;
  - боевой сервер на 27.07.2026: PHP 7.2 в терминале (код платформы синтаксис PHP 8 не использует, работает; но 7.2 EOL с 2020 — поднять до 8.1; cron идёт по терминальной версии), состояние 5.1 МБ из них PLANSVG 3.1 МБ, 62 помещения без коммерческих данных, 18 помещений в корзине, Hilola числится в отказах и как AG (без `registry`), и как HO (с `registry`) — возможно, роль меняли;

- **v4.49.2: варианты не теряются при подборе + справочник категорий втрое больше:**
  - **КОРЕНЬ «варианты брендов пропадают после подбора»**: ячейка «Варианты» в regEditRow — это `<input list="brandsDL">` с value=vars.join(', '). Выбор из datalist в Chromium заменяет ВЕСЬ value, а не последний токен → 5 брендов превращались в 1. Улика в истории у пользователя: «Варианты (реестр): очищено», затем «Варианты (реестр): е». ФИКС в saveVarsLive (core.js): если список укоротился на ≥2 при прежних ≥2 — confirm, где OK = ДОБАВИТЬ новые к прежним (merged), Отмена = заменить. Обычные ±1 без вопросов. Тест v4492_vars_guard.js 6/6;
  - **ДВА РАЗНЫХ СПРАВОЧНИКА SUBS** (важно, легко ошибиться): `core.js` SUBS = 17 категорий реестра помещений (CASECATS); `v35-stable.js` SUBS = 16 категорий БАЗЫ БРЕНДОВ (своя таксономия: 'Услуги и сервисы', 'F&B / рестораны и кафе'…). Жалоба про бренды → правится v35-stable. Оба расширены: бренды 97→288 подкатегорий, реестр 77→202. Полиграфия добавлена в «Услуги и сервисы» и в «Услуги, киоски, спец. магазин». Ничего не удалено — старые значения карточек целы;
  - поля категории/подкатегории — `<input list=...>`, свободный ввод РАЗРЕШЁН всегда (не select); справочник только подсказывает;
  - core.js реально изменился → его ?v= поднят до 4.49.2 (правило кэша соблюдено);
  - прогон 187 проверок зелёный.
- **v4.49.1: группы «Бюджет/Факт» + ГЕОДАННЫЕ БОЛЬШЕ НЕ ПРОПАДАЮТ МОЛЧА:**
  - СВОРАЧИВАНИЕ ГРУПП: кнопка «Факт» убирала 9 столбцов вместо 5 (Варианты/Брокер/Комментарий пропадали). Причина — ДВА механизма скрытия: легаси-правило в index.html `table.reg-grp.cg-fac-collapsed th[data-colkey^="f_"]{display:none}` убирало ячейки ИЗ СТРОКИ, а v432 (effectiveHidden уже сам знает про groupState!) дополнительно ставил `visibility:collapse` на их `<col>`. Строка становилась короче colgroup → оставшиеся ячейки съезжали на обнулённые слоты. ФИКС: легаси-правила (bud+fac) УДАЛЕНЫ, сворачивание целиком за v432. Тест v4491_groups.js 14/14;
  - **ГЕО-СОХРАНЕНИЕ, жалоба «у младшего админа пропадают данные, не исчезает»**: замер на моке дал таблицу — 409 и 403 честные (тост+черновик), а `!canEdit()` → ПОЛНАЯ ТИШИНА: ни тоста, ни POST, ни черновика (`if(!canEdit()||G.saving)return;`). Сотрудник вводит, жмёт «Сохранить», работа исчезает бесследно. ФИКС: оба условия теперь с сообщением + черновик в `case_os_geo_pending_v1`;
  - **ЗАЩИТА ОТ ОБВАЛА**: гео сохраняется ВСЕМ массивом сразу, неполный пакет затирал базу. Теперь при падении числа записей более чем вдвое (и было >20) — confirm с цифрами «N вместо M, потеряется K»; отказ = сервер не тронут. Тест v4491_geo_guard.js 4/4 (148 записей спасены, обычная правка без вопросов);
  - ВОССТАНОВЛЕНИЕ для пользователя: `geo_state.php?history` — 50 снимков на сервере, админ откатывает через «История / восстановление»; плюс «Восстановить локальную копию» из localStorage;
  - изъян v4.49.0 исправлен: CASE_EXPECTED_MODULES лежала в core.js → ядро менялось при любом обновлении модуля и не кэшировалось. Перенесена в index.html. Проверено: ядро осталось на ?v=4.49.0 и берётся из кэша;
  - прогон 208 проверок зелёный.
- **v4.49.0: ЯДРО ВЫНЕСЕНО из index.html в os/core.js** (страница 1004 КБ → 56 КБ):
  - обоснование замером: index.html за 9 релизов менялся на 4–25 строк, из них 4–17 — версия и теги; сама логика ядра менялась в 3 релизах из 9 на 4–8 строк. При этом APP_VERSION в шапке меняется всегда → браузер перекачивал весь мегабайт впустую;
  - механика: блок 1 (838 КБ кода, был на строке 448 ДО тегов модулей) вынесен в core.js; на его месте `<script>const APP_VERSION=...</script><script src="core.js?v=4.49.0"></script>` — БЕЗ defer, чтобы порядок исполнения не изменился ни на шаг (inline-блок 2 на строке 4390 исполняется при парсинге; с defer ядро запустилось бы ПОСЛЕ него и всё бы сломалось);
  - APP_VERSION намеренно оставлен в index.html: уехал бы в ядро — ядро менялось бы каждый релиз, выигрыш обнулился;
  - `CASE_EXPECTED_MODULES` и caseCheckModuleVersions переехали в ядро вместе с кодом (там же теперь запись 'core'); core.js добавлен в ASSETS sw.js;
  - `document.write` в ядре (4 шт.) — это `w.document.write` в НОВОЕ окно печати, для выноса безобидно; проверено заранее;
  - ТЕСТЫ: p01_vm_test / verify_full_qa / v4440_reliability читали код из index.html — теперь читают `index.html + core.js` (существование core.js проверяется, работают и со старой структурой). v4461_poll_sync не тронут: он ищет APP_VERSION, а тот остался в index.html;
  - прогон 245 проверок зелёный + смоук ядра (запрос 200, вход, 9 разделов, 0 ошибок);
  - ДЛЯ ДЕПЛОЯ: core.js — НОВЫЙ файл, без него пустая страница. В инструкции это выделено.
- **v4.48.4: аудитор конфликтов CSS + кнопки на телефоне в Базе брендов:**
  - НОВЫЙ ИНСТРУМЕНТ `docs/qa/tools/css_conflicts.js` — то, чего не хватало все релизы v4.46–v4.48. Поднимает платформу, обходит 8 разделов (стили инжектятся лениво!), читает ЖИВЫЕ document.styleSheets. ДВА принципиальных решения, без которых он бесполезен: (1) декларации парсятся из авторского `r.cssText`, а НЕ из `r.style` — иначе один `background:` разворачивается в 9 мнимых конфликтов (первая версия дала 361 «конфликт», мусор); (2) конфликт засчитывается ТОЛЬКО если `querySelectorAll(selA)` и `querySelectorAll(selB)` пересекаются на реальном элементе — иначе «оба про th, но в разных таблицах» лезут в отчёт. Валидация: пункт №1 в выдаче = ровно тот баг из v4.48.3 (`.ux-table-viewport thead th{top:0!important}` vs leafrow th), который искали три релиза;
  - карта техдолга по стилям: 188 подтверждённых конфликтов с !important, из них 56 ломающих вёрстку. Главные пары: asaas35-css ↔ asaas3513-ux-css (46), caseGridCss4325 ↔ inline-в-index.html (39), asaas3513-ux-css ↔ caseGridCss4325 (33). Разбирать постепенно;
  - ИСПРАВЛЕНО по находке №5: `.ux-brand-controls .btn{min-height:32px!important}` (v3515-ux, спец. 0,2,0) перебивало правило доступности `.btn{min-height:44px!important}@(max-width:820px)` (v4450, спец. 0,1,0) → в Базе брендов на 390px было 7 кнопок по 32px. Фикс: те же селекторы явно добавлены в медиазапрос v4450. Замер до/после: 7 → 0;
  - регресс 179 проверок зелёный.
- **v4.48.3: полоса «Бюджет/Факт» прилипает** («при сдвиге вниз строка бюджет и факт уходит»):
  - div.case-grp-band — дублёр группового ряда: обычный блок В ПОТОКЕ wrap (перед таблицей, height=grpH, margin-bottom:-grpH), sticky top:0 z10 → горизонтально едет с таблицей САМ (никакой синхронизации), вертикально липнет; ширины сегментов из РЕАЛЬНЫХ rect'ов bud/fac ячеек (не из col style — min-width:100% растягивает); innerHTML bud/fac клонируется (лейблы «▾ Бюджет»), клик проксируется реальным ячейкам (regToggleGrp); строится в buildGrpBand() из applyPins + syncOneStickyTable;
  - ряд колонок липнет ПОД полосой: leafrow th top:var(--cg-group-h,24px)!important;
  - **НАЙДЕН ГЛАВНЫЙ ИСТОРИЧЕСКИЙ ВИНОВНИК**: v3515-ux (sheet asaas3513-ux-css) правило `.ux-table-viewport thead th{top:0!important;position:sticky}` — ГЛУШИЛО все вертикальные смещения рядов шапки во всех версиях (из-за него «не работали» родные top-правила и был придуман клон!). Диагностика таких вещей: перебрать document.styleSheets и найти правила с th.matches(sel) и style.top — смотреть PRIORITY;
  - тест v4463 → 13 проверок (полоса stuck/±1px/painted, leaf под полосой) ×3; v4441 ожидание top обновлено (0..30). Батарея 194 зелёная + скриншот обеих прокруток.
- **v4.48.2: УГЛОВОЙ БЛОК закреплённых заголовков** («столбцы закрепляются, но шапка нет + белые пятна»):
  - ФАКТ CHROMIUM №2 (инструментально: elementFromPoint в центре rect пин-th вернул СОСЕДНЮЮ прокрученную ячейку): ячейка таблицы, прилипшая по ДВУМ осям, НЕ отрисовывается по геометрии — rect/computed правильные, paint и hit-test старые. То же с transform на th (matrix есть, пикселей нет) и absolute-двойником ВНУТРИ th. НЕ ДОВЕРЯТЬ rect'ам ячеек — проверять elementFromPoint;
  - решение: div.case-pin-corner — первый ребёнок .tbl-scroll, position:sticky;top:0;left:0 (двухосевой sticky НА DIV работает), height=шапка, margin-bottom:-height (не сдвигает таблицу), z-index:16; содержит клоны pinned leaf th (ширины из rect, innerHTML), строится в buildPinCorner() из applyPins; clearPins удаляет; прокси: pointerdown резайзера → реальному по data-case-col-id, click → реальному th/кнопке;
  - пины ШАПКИ больше не sticky-left (left:auto!important в thead-правиле) — их показывает угол; пины ТЕЛА (td) — native sticky left как раньше (одноосевой td рисуется корректно);
  - «белые пятна»: grprow th bg #fff → var(--cg-head);
  - тест v4463 переписан: 11 проверок с elementFromPoint-валидацией отрисовки + резайзер ИЗ УГЛА; 11/11×3, батарея 192 зелёная + скриншот-контроль.
- **v4.48.1: шапка держится по ОБЕИМ осям + атомарное скрытие столбцов** («не фиксируется шапка», «влево-вправо сбивается»):
  - sticky-thead (4.48.0) отставал от тела по X у пользователя → прилипание перенесено на per-th ряда КОЛОНОК: leafrow th{top:0} sticky, групповой ряд grprow th{position:relative!important} НЕ липнет (уезжает — штатно). ФАКТ CHROMIUM: разные top у sticky-ячеек многорядного thead ИГНОРИРУЮТСЯ (все липнут на минимальный top, ряды накладываются) — поэтому липнет ровно один ряд; горизонталь у per-th идеальна по построению (ячейка привязана к колонке);
  - ВТОРОЙ реликт-виновник: стиль colrzCss (легаси-резайзеры index.html) `table[data-tblkey] thead th{position:relative}` путал каскад — inline-top'ы теперь чистятся в syncOneStickyTable, top задаёт CSS leaf-правило top:0;
  - СКРЫТИЕ СТОЛБЦОВ ПЕРЕПИСАНО: только `col{visibility:collapse}` (dbg: col display:none в Chromium НЕ скрывает ячейки, collapse — скрывает th+td атомарно!); applyVisibility больше НЕ трогает display у th/td (старые display:none вычищает); adjustGroupedHeader упрощён: colspan групп всегда ПОЛНЫЙ (collapse-колонки считаются в colspan), подпись группы visibility:hidden только когда вся группа скрыта, ячейкам группового ряда display не трогается → индексные сдвиги «шапка без М², тело с М²» невозможны по построению;
  - тесты: v4463_pins 10 проверок (leaf-ряд у кромки, групповой ряд НЕ sticky, линии ±1, скрыть/показать столбец синхронно — ВНИМАНИЕ: label «м²» искать по /^м²/ (просто /м²/ матчит «$/м²» у всех бюджетных)), 10/10×3; v4441 8/8; вся батарея 190 зелёная;
  - ЛОВУШКИ ДИАГНОСТИКИ (повторялись!): rect СТРОКИ (tr) не показывает прилипание — липнут ячейки, мерить th; collapsed-ячейки имеют display:table-cell но width 0 — фильтровать по width>0.5.
- **v4.48.0: КЛОН ШАПКИ УДАЛЁН — нативный sticky («всё сбивается при малейшем движении»):**
  - весь клон-механизм (слой, headSignature, proxy-обработчики) ВЫРЕЗАН из v432; вместо него: (1) capMainGridWrap — обёртка главного грида получает maxHeight по экрану → вся вертикальная прокрутка ВНУТРЕННЯЯ; (2) `table.case-grid>thead{position:sticky;top:0;z-index:11}` — липнет ВЕСЬ thead; (3) горизонтальные пины как раньше (.case-grid-pin sticky left);
  - ДВА ИСТОРИЧЕСКИХ ВИНОВНИКА, из-за которых родные sticky-правила (лежали в CSS v432 годами!) не работали и был придуман клон: (а) addGenericResizers ставил КАЖДОМУ th инлайновый position:relative — глушил sticky (убран, th.style.position=''); (б) БАГ CHROMIUM: sticky на отдельных th ВЕРТИКАЛЬНО не работает в этой таблице (fixed+colgroup) — изолированный тест th работает, в реальной нет; sticky на ЦЕЛОМ thead работает. Диагностика: форсировать inline sticky на th (не липнет) vs на thead (липнет);
  - сохранены: handoffWheel/installScrollHandoff (передача колёсика странице), --cg-group-h, resize/scroll → recap высоты (rAF);
  - syncStickyTables теперь: убирает чужие .case-sticky-head-layer из DOM (старые модули), ставит --topbar-h, capMainGridWrap + handoff на все case-grid;
  - ТЕСТЫ ПЕРЕПИСАНЫ под нативную модель: v4463_pins 9 проверок (thead прилип у кромки wrap, линии th vs td ±1px, пины при верт+гор, резайзер В ПРИЛИПШЕМ состоянии, ноль клон-слоёв), v4441 сценарий 2 — резайзер в реальном thead. 9/9×3 + вся батарея 180 проверок зелёная;
  - UX-примечание: главные гриды теперь скроллят строки ВНУТРИ себя (высота по экрану) — как AG Grid; страница прокручивается только до таблицы.
- **v4.47.1:** «линии сбиваются при прокрутке вниз» — боковая рамка 1px у .case-sticky-head-layer сдвигала весь клон на 1-2px вправо от тела (замер: постоянные +2px по ВСЕМ границам). Фикс: border:0 + border-bottom у слоя (v432 injectCss). Тест v4463_pins расширен до 9 проверок (линии клона vs тело ±1px, групповой ряд) — 9/9×2. Урок: клон в fixed-слое — ЛЮБАЯ рамка/паддинг слоя = смещение всех линий; мерить `right`-границы видимых ячеек клона против той же колонки тела.
- **v4.47.0: ПЕРЕСБОРКА ТАБЛИЧНОГО ЯДРА (план из v4.46.3 выполнен, запрос «пересобрать все таблицы» + «исправь сразу»):**
  - весь sticky-блок (stickyTopRO/…Raf, pageScroller, topbarHeight, hasVerticalRange, parentVerticalScroller, destroy/ensure/updateOne/updateAllSticky, cloneHeaderTable, headSignature, proxyStickyClick/ResizerDown, scheduleStickyUpdate, syncOneStickyTable, handoffWheel, installScrollHandoff, syncStickyTables, export window.CASE_SYNC_STICKY) ПЕРЕЕХАЛ из v4327-patch.js в v432-data-grid.js (перед window.case432EnhanceNow), вместе с 3 CSS-правилами (.case-sticky-head-layer{}, .case-sticky-head-layer table{}, sticky-source th:not(.case-grid-pin)); в v4327 postRender теперь зовёт window.CASE_SYNC_STICKY через typeof-guard; v4327 остался: очередь unit_patch/batch, adaptiveActions, broker picker, external-agent, chipbar;
  - **страховочный CSS в index.html** (в первом <style>): `table.case-grid.case-page-sticky-source>thead th.case-grid-pin{position:sticky!important}` (спец. 0,3,3 бьёт старое модульное правило 0,2,3 независимо от порядка) + клэмп контролов в ячейках. Доказано смешанной сборкой: os_mixed = новый os + `git show 72e95fe:case-site/os/v4327-patch.js` (v4.46.2) → v4463_pins 6/7 (падает только клон из старого файла, о нём кричит сверка версий);
  - полная батарея 117 проверок зелёная (pins 7/7×3, visual 9/9, poll 12/12, ui 8/8, rel 14/14, live 5/5, smoke 8/8, scen 27/27, roles 54/54, QA 34/34);
  - ВАЖНО про два клона в смешанном режиме: старый v4327 создаёт СВОЙ слой поверх нового — деградация видна только в клоне, основная шапка работает от страховочного CSS.
- **v4.46.4:** самовосстанавливающийся клон шапки + контроль неполной заливки («всё равно та же проблема»):
  - ГОНКА КЛОНА (поймана тестом v4463_pins, флейк 5/7→7/7×3): enhanceAll вызывал CASE_SYNC_STICKY синхронно, а applyPins/adjustGroupedHeader живут в rAF внутри enhanceTable → клон строился ДО пинов/ширин и НЕ пересобирался (ensureStickyClone один раз на sync) → поверх настоящей шапки лежала копия без пинов/со старыми ширинами — вероятно ЭТО скрин пользователя «шапка съехала на столбец». Фикс: (а) headSignature(tb) — отпечаток шапки (className+width+col widths/display+пины left+hidden) хранится в layer.dataset.caseSig, updateOneSticky пересобирает клон при изменении; (б) enhanceAll зовёт CASE_SYNC_STICKY через requestAnimationFrame (после rAF-этапов enhanceTable);
  - СВЕРКА ВЕРСИЙ ФАЙЛОВ: 6 модулей (v432-data-grid, v4327-patch, v4451-live-sync, v3520-workspaces, v4450-ux-system, v4450-owner-report) в конце файла пишут CASE_MODULE_VERSIONS['имя']='4.46.4'; index.html через 2.5с после load сверяет с CASE_EXPECTED_MODULES и при рассинхроне тостит 15с «Перезалейте /os целиком» + console.warn. Ловит главный кейс ручного деплоя: залили index, забыли модуль (симптом пользователя совпадал с v4.46.2-багом = старый v4327-patch на хостинге). ПРИ КАЖДОМ РЕЛИЗЕ: поднимать версии в CASE_EXPECTED_MODULES только для реально изменённых файлов НЕЛЬЗЯ — маркер пишется в конец файла при каждом релизе всем шести (проще: обновлять все 6 маркеров + карту разом; в этом релизе все = 4.46.4);
  - демо-репро строк тела: все ровные (21 ячейка), группировок в tbody нет — индексных расхождений applyPins в текущем коде не найдено.
- **v4.46.3:** закрепление столбцов работает целиком («фиксируется только тело, шапка нет + белые пятна»):
  - КОРЕНЬ: конфликт слоёв. v432 applyPins даёт th/td класс `.case-grid-pin{position:sticky!important}` (спец. 0,2,1) + inline left; правило v4327 `table.case-grid.case-page-sticky-source>thead th{position:relative!important}` (спец. 0,2,3, грузится позже) ПЕРЕБИВАЛО sticky у th → заголовки со сдвигом left при relative «уезжали» = белые пятна. Фикс: `:not(.case-grid-pin)` в правиле v4327;
  - ВТОРОЙ КОРЕНЬ: клон шапки (вертикальное прилипание) синхронился `head.style.transform=translateX(-scrollLeft)` — внутри transform sticky НЕ работает вообще. Фикс: слой = настоящий скролл-контейнер (`layer.scrollLeft=wrap.scrollLeft`, transform убран; overflow:hidden у слоя уже был) — клонированные .case-grid-pin th (классы копируются) прилипают сами;
  - диагностика таких багов: мерить `getComputedStyle(th).position` + `th.getBoundingClientRect().x - wrap.x` у pin-ячеек шапки И тела при scrollLeft>0 — должны совпадать и быть sticky;
  - тест docs/qa/tools/v4463_pins.js 7/7 (закрепление через настоящую панель «Столбцы» → [data-pin]/[data-save]); полный регресс зелёный, вкл. v4441 (резайзер из клона).
- **ПЛАН v4.47.0 — консолидация табличного стека** (запрос пользователя «надо пересобрать все таблицы, а то друг на друга пишем коды»):
  - сейчас три слоя: v432-data-grid (вид/ширины/пины/фильтры/тулбар), v4327-patch (клон шапки + adaptiveActions + очередь unit-синка), v4451-live-sync (точечные перерисовки). Последние баги — ВСЕ на стыках (CSS-специфичность, transform vs sticky, debounce vs render);
  - план: (1) вынести из v4327 всё табличное (клон шапки, adaptiveActions, syncTblScroll-взаимодействие) в v432 → один владелец презентации таблиц; v4327 остаётся только очередью unit_patch/flush; (2) в v432 единый пайплайн enhance: order→density→widths→visibility→resizers→pins→sticky→toolbar, вызываемый и по debounce, и синхронно (case432EnhanceNow); (3) публичные API сохранить: window.case432EnhanceNow, window.CASE_SYNC_STICKY, window.caseFlushUnitsNow/caseUnsyncedCount, формат префов caseos_grid_prefs_v4322 (миграция не нужна); (4) прогнать ВСЕ наборы + v4463_pins + v4462_visual; (5) отдельный релиз, без смешивания с фичами;
  - страховка: тесты уже покрывают стыки (пины+клон 7/7, клон-резайзер, флэш-кадр, поза при live-sync).
- **v4.46.2:** стабильный вид таблиц (3 жалобы дня):
  - «мигает то старый то новый вид»: после live-sync перерисовки таблица жила без улучшения до дебаунса (~90-600мс) → v432 экспортирует `window.case432EnhanceNow=enhanceAll`, live-sync withPreservedUi вызывает его СРАЗУ после fn() (до restore) — кадра «сырой» таблицы нет. Вторая половина: свои правки через unit_patch не попадают в _syncedKeyJson → опрос считал их «чужими» и тостил — тост подавлен при `updated_by===S.user.name` (перерисовка остаётся, она теперь невидима);
  - «селекты за линию выходят»: статус-селект классической строки ЛСР (index.html ~1869, class st-sel) имел `max-width:150px` БЕЗ width:100% → при столбце <150px торчал на 40px (мерить: sel.getBoundingClientRect().right - td...right). Фикс: width:100%+box-sizing там + страховка в v432 CSS `table.case-grid td select,td input:not([type=checkbox]),td textarea{max-width:100%!important;box-sizing:border-box!important}`;
  - «у администратора тыла (роль HO!) нет настроек таблиц»: в текущей v432 гейта НЕТ — «Столбцы»/«Вид» у всех ролей на всех основных таблицах; урезанная панель на скриншоте пользователя = его старый v4.45.x билд на хостинге (там canColumns гейтился). Решение = обновление; закреплено тестом (HO: registry/brands/dates полная панель);
  - тест docs/qa/tools/v4462_visual.js 9/9; v4461_poll_sync теперь версионно-независим (кэш выводится из APP_VERSION). Серверные PHP не менялись.
- **v4.46.1:** живая синхронизация без перерисовок — влит билд пользователя v4.45.1:
  - **v4451-live-sync.js** (из билда пользователя, целиком): capture/restore UI (позиции всех прокруток по ключам `table:<data-tblkey>`, фокус+курсор, details[open]; restore с ретраями rAF+40/120/260/520мс, токен restoreSeq гасит старые цепочки); VIEW_KEYS (раздел→ключи состояния); refreshAfterServer(changed) — перерисовка ТОЛЬКО если изменение касается текущего раздела; entitySaved(kind,id) + событие `caseos:entity-saved`. МОЁ дополнение: универсальный фолбэк entitySaved на остальные разделы (кроме geo/feas-iframe) — их unitSave обновлял только registry/plans/dash/map;
  - index.html: pollServerState считает `_serverChangedKeys` (сравнение с _syncedKeyJson) → refreshAfterServer, `_rememberSynced` после applyState, тост только при changed.length; isUserBusy += `_saveBusy||_savePend||_offlineDirty||_lastLocalMutationAt<2.5с` + модалки `[role=dialog]`; persist() штампует _lastLocalMutationAt; unitSave/brandSave(legacy)/brandTouch/brandMerge-ветка → entitySaved (фолбэк refreshViewKeepScroll);
  - v35-stable: asaas35SaveBrand → entitySaved/renderBrandsResultsOnly (лёгкий ререндер результатов, НЕ полный renderBrandsStable), экспорт window.asaas35RefreshResults;
  - v420-geo-studio: geoUiSnapshot/geoUiRestore вокруг refreshAll;
  - **НАЙДЕН СКРЫТЫЙ БАГ (только backend!)**: первый клик «✎ Редактировать помещение» → brokerNames() → loadSrvUsers() → `_rerenderUsersOrOrg()` МОЛЧА перерисовывал ЛСР за карточкой → scrollLeft=0. В demo не воспроизводится (SRV_USERS не грузится) — потому v4441 его не ловил. Фикс: loadSrvUsers не дёргает экран если данные не изменились (JSON-сравнение), при изменениях — withPreservedUi;
  - ТЕСТ: docs/qa/tools/v4461_poll_sync.js 12/12 (мок-бэкенд; mock_backend.js теперь отдаёт `state.appState.updatedAt` — менять серверное состояние «чужой рукой» через /__ctl {appState:{data,revision,updatedAt}}). Полный регресс 13 наборов зелёный. Серверные PHP в этом релизе НЕ менялись.
- **v4.46.0:** слияние с параллельным билдом пользователя (v4.45.0_FULL_HOSTING_READY) — «собери сам хороший вариант»:
  - взято из билда пользователя: **v4450-owner-report.js** (Отчёт собственнику: профили owner/internal, вкладки summary/areas/brands/refusals/plan/quality/versions, кнопка через MutationObserver, view `owner_reports`, state-ключ OWNER_REPORTS) + **v4450-ux-system.js** (caseStrictCanOpen-гард поверх go(), caseCanEditRegistry, экран «Нет доступа», a11y, адаптивный CSS, бейдж «Только просмотр»);
  - **fail-closed модель ролей** (принята КАК ЗАДУМАНО пользователем — отменяет прежний курс «вернуть BRJ реестр»): AGX только ['dash','work_tasks','work_kanban','brands','v32_investors']; BRJ — + гео/рынок/качество/импорт, но БЕЗ registry/case_projects; BSH — дефолт + registry только чтение. Сервер: lib.php `asaas_workspace_hard_allowed`, unit_patch/units_batch → 403 для AGX/BSH/BRJ; клиент: v3520 hardAllowed/canOpen + миграция схемы **4450** (union с DEFAULTS, затем fail-closed обрезка, включая кастомные USER_WORKSPACES);
  - state.php — файл пользователя целиком: OWNER_REPORTS (создатели ASH/CFO/BA/HO) + `sanitize_owner_report_snapshot` (вычищает broker/rate/rent/comment/contacts/ownerContact/internalComment/commission из снапшотов);
  - сохранено моё из v4.44–4.45: сессия 12ч + keepalive, дозаливка очереди, refreshViewKeepScroll, резайзеры в прилипшей шапке, гео-эталон таблиц, серверные per-user настройки таблиц;
  - index.html: OWNER_REPORTS в stateBlob/applyState/labels; canEdit-гейты реестра через caseCanEditRegistry (ловушка: у пользователя и у меня был свой `const canEditVars` в openUnit — дубль объявления ломал ВЕСЬ inline-скрипт, старую строку удалить);
  - СЕРВЕРНЫЕ файлы обновлены: lib.php, state.php, unit_patch.php, units_batch.php. После деплоя нужен ОДИН вход администратором для миграции 4450;
  - тест docs/qa/tools/v4460_merge_smoke.js 8/8 + полный регресс всех наборов зелёный (см. CHANGELOG v4.46.0). Аудит-доки билда пользователя сохранены в docs/audit/v4.45.0/, его QA — docs/qa/v4.45.0/.
- **v4.45.0:** карточки→таблицы сразу + резайзер в прилипшей шапке + гео-эталон вида:
  - refreshViewKeepScroll() (index.html): go(S.view) с восстановлением scrollLeft/Top всех .tbl-scroll и window; применён ко ВСЕМУ карточному семейству юнита (unitSave/doMerge/doSplit/addVar/delVar/отказ/saveUnitDate/extendOffer/переговоры/editUnitArea/addComment/addUnitCatV/delUnitCatV), legacy brandSave, scrSetSalePrice/Status (v32). Актуальный редактор брендов (v35 asaas35SaveBrand) уже перерисовывал сам;
  - прилипшая шапка: cloneHeaderTable БОЛЬШЕ НЕ вырезает .case-grid-resizer; CSS display:block; proxyStickyResizerDown на layer пробрасывает pointerdown реальному резайзеру по data-case-col-id + scheduleStickyUpdate на move («пропала регулировка ширины» — в клоне резайзеры были display:none);
  - гео-эталон (v432): плотность по умолчанию comfortable = гео-метрики (th 9×10 #faf8f5 #625d56 w750; td 9×10 h40), hover-подсветка, подпись тулбара «Показано N строк · M столбцов»;
  - тест v4441_ui_flows.js 8/8 (в карточку входить через кнопку «Редактировать»; бренды — bd_* поля + asaas35SaveBrand; клон-резайзер — dispatch на сам rz). ПОЛНЫЙ прогон 15 наборов — всё зелёное (см. CHANGELOG).
- **v4.44.0:** корень «данные не сохраняются» (ЛСР/гео) — истечение серверной сессии (1 час) + тихие отказы:
  - lib.php: gc_maxlifetime/cookie 3600→43200 (12ч);
  - index.html: keepalive-пинг auth.php каждые 5 мин при активности (_lastActivityAt; auth:false → sessionExpired сразу); IDLE_LOGOUT_MIN 60→120; apiGET/apiPOST кидают Error с .status; sessionExpired показывает счётчик caseUnsyncedCount(); enterWithServerUser ПЕРЕД apiLoadState дозаливает: await caseFlushUnitsNow() + (_offlineDirty && _doSaveState()) — иначе applyState затирал несохранённое;
  - v4327: flushUnit/flushUnitBatch различают 401 (держим очередь, один тост, дозальётся после входа) / 403 (очистка + тревожный тост 10с, БЕЗ вечных повторов) / сеть (ретрай 5с как раньше); window.caseUnsyncedCount, window.caseFlushUnitsNow;
  - syncTblScroll: полоса ищется среди предыдущих соседей до ближайшего .tbl-scroll, дубликаты удаляются (тулбар грида вставал между полосой и таблицей → рисовались ДВЕ полосы);
  - ТЕСТ-ИНФРА: docs/qa/tools/mock_backend.js — мок PHP-бэкенда (sessionValid/failMode через /__ctl, инспекция /__inspect); v4440_reliability.js 14/14. Сид юнитов обязан содержать dates:[] (boot→allDates падает без него).
  - Регресс: 34/34, 27/27, live 5/5, роли 54/54.
- **v4.43.3:** правки в таблицах видны сразу («приходится перегружать страницу»):
  - saveCellLive сохранял, но не обновлял DOM (полный ререндер украл бы фокус) → вычисляемые колонки и «% занято» жили до reload;
  - решение: `<tr data-uid>` в regEditRow + regLiveRefresh(u,field) — точечное обновление НЕ-редактируемых ячеек строки (lcrBudVis/lcrFacVis → lcrCellVal) и бейджа занятости; инпуты не пересоздаются, фокус сохраняется;
  - тест v4433_live_edit.js 5/5 (b_tot/b_tots пересчёт, инпут тот же); полный QA 34/34, сценарии 27/27.
- **v4.43.2:** гео-переход доведён + «полоска» на LCR:
  - карта перелетает к проекту при «Аналитике» (fly + повторы 700/2000мс — первичное центрирование проектов перебивало); ВАЖНО: в студии «map» — top-level let, window.map — это <div id=map> (named access, без setView!) — обращаться только к лексическому map;
  - переоткрытие отчёта: кнопка «🎯 Отчёт по точке» в попапе маркера проекта (geoObjProbe), клик по красному маркеру отчёта;
  - «полоска» на таблицах: `table.case-grid.case-page-sticky-source>thead th{position:static!important}` (v4327 sticky) отрывал якорь .case-grid-resizer → 22 резайзера высотой во всю таблицу; исправлено на relative + чистка осиротевших резайзеров + th{position:relative} в grid CSS;
  - ШТОРМ: adaptiveActions (кнопки+«⋯ Ещё», v4327) зацикливался с ResizeObserver — 782 мутаций/сек на «План/бюджет», из-за чего v432 schedule (дебаунс 90мс) голодал и таблица вкладки не улучшалась вовсе. Переписан: кэш ширин, математический расчёт состава, DOM только при изменении, гистерезис 28px, RO с проверкой смены ширины. Итог 0 мутаций/сек. В schedule добавлен максимум ожидания 600мс;
  - тесты: v4432_probe_flow 10/10, полный QA 34/34, сценарии 27/27, ширины PASS.
- **v4.43.1:** гео: авто-отчёт по проекту + PDF:
  - «Карта объектов → Аналитика» (case493OpenGeo) → geoV42Project(id,{probe:true}) → context.probeProjectId (одноразовый, очищается после отправки) → studio applyContext → geoAutoProbe (дедуп GEO_AUTOPROBED, retry-wait на map/probeAt) → probeAt(coords проекта) — «Отчёт по точке» открывается сам; обычное открытие гео из меню НЕ триггерит;
  - probeAt теперь собирает тело отчёта в LASTPROBE.html; новая кнопка «⤓ Скачать PDF» → exportProbePdf(): window.open + печатная версия (шапка CASE, проект, дата, все разделы) + print() → «Сохранить как PDF»;
  - тест v4431_probe_pdf.js 7/7 (авто-открытие, состав, PDF-контент, print(), дедуп). Регресс: uniform 14/14, bc 8/8, полный QA 34/34.
- **v4.43.0:** личные настройки таблиц — на сервере, per-user («ширины сбрасываются» + «каждому по-своему»):
  - новый `api/user_prefs.php` (GET/POST только СВОИХ настроек по user_id; CREATE TABLE IF NOT EXISTS user_prefs — ручных миграций нет);
  - v432-data-grid: maybePullServerPrefs() в enhanceAll (один pull за сессию, backendOn()=BACKEND+S.user), savePrefs → pushServerPrefs (debounce 800мс, dedup по JSON); конфликт: локальные правки текущей сессии поверх серверных;
  - localStorage остаётся кэшем; в demo-режиме поведение прежнее (без сервера);
  - тесты: v4430_prefs_sync.js 3/3 (pull/push/«чистое устройство»), width_persist2.js (локальная стойкость), полный QA 34/34.
- **v4.42.3:** починены подписи БЦ/медицины на карте («галочка есть, но не сработало»): в Leaflet 1.9.4 ПОВТОРНЫЙ bindTooltip до добавления маркера на карту молча ломает permanent-подпись; renderBC/renderMed биндят подпись теперь ровно один раз (permanent ЛИБО sticky). Аптеки/POI были с одинарным биндом и работали. Репро-тест: 0 → 79 подписей БЦ / 180 мед (docs/qa/tools/bc_labels_repro.js). ВАЖНО для будущего кода карты: никогда не вызывать bindTooltip дважды на одном маркере до addTo.
- **v4.42.2:** единые настройки категорий гео-студии (по скриншоту пользователя «у БЦ нет кнопки подписи + лишняя галочка»):
  - шапка каждого слоя = только включатель+название (+⚙ от buildPanel); ВСЕ контролы — подписанные, в sub-строках (buildPanel прячет их в ⚙-панель; голые input в шапке — то, что осталось в главной .styrow);
  - БЦ: bcNa/bcLab перенесены в sub с подписями «цвет»/«подписи»; медицина: +medLS (размер подписи), +medClu (кластеры, swap gMed c map.hasLayer); аптеки: +phLS (свой стиль .phlab вместо общего .medlab), +phClu; метро mLab/mByLine, дороги rByLine, районы dLab — подписаны словами; плотность/тепловая — «цвет»/«прозр.» в sub;
  - тест `docs/qa/tools/v4422_studio_uniform.js` (14/14): нет голых контролов в .lrow, полные наборы, кластеры мед (1530 т.) и аптек (1027 т.) вкл/выкл, phlab 16px. Регресс: bc 8/8, полный QA 34/34.
- **v4.42.1:** лечение «данные не сохраняются» у сотрудников (BRJ: гео/бренды; администратор аренды: ЛСР):
  - корень: сервер принимает изменения только разделов из workspace роли; сохранённые шаблоны ролей были «снимками» узких умолчаний v4.35 → geo_state.php отдавал 403, state.php тихо восстанавливал старые значения (rejected_keys никто не показывал);
  - миграция **4421**: шаблон КАЖДОЙ роли одноразово дополняется её DEFAULTS (расширение точечной миграции 4420);
  - клиент показывает `rejected_keys` из ответа state.php: `_warnRejectedKeys` в index.html («⚠ НЕ сохранено: …» по правам; «↻ Помещения …» по конфликту updatedAt), дедуп за сессию, toast(msg,ms) с длительностью;
  - серверные файлы не менялись. Тесты: v4421_check 7/7, сценарии 27/27, полный QA 34/34, роли 54/54.
- **v4.42.0:** пакет исправлений по замечаниям пользователя:
  - меню не сворачивается на «Версиях планировок» (последний вызов легаси `caseNavClose` заменён на `caseNavSync`; сам `caseNavClose` — no-op);
  - таблицы data-grid не «сжимаются»: при сумме ширин столбцов меньше карточки таблица растягивается на 100% (`min-width:100%` в `syncTableWidth`, v432);
  - **миграция workspace schema 4420**: сохранённый шаблон роли BRJ дополняется дефолтным набором (гео/бренды/реестр/рынок) — лечит прод, где шаблон был записан без них; миграция вызывается из `buildNav()` ПОСЛЕ загрузки состояния и только при вошедшем пользователе (ранний `persist()` затёр бы данные);
  - «Права ролей» объединены с матрицей разделов: отдельная таблица удалена из `index.html` (`renderRoleMatrix` больше нет), тумблеры прав — блок «Права на действия» в `wsAdminCard` (v3520, `rightsBlock`/`wsToggleRight` → общий `toggleRight`);
  - БЦ в гео-студии = HORECA-набор контролов: `bcLS` (размер подписи 8–20), `bcClu` (кластеры, markerClusterGroup, флаг `gBC._caseClu`);
  - мини-тест: бейдж справа над чатом (right 18 / bottom 86), скрыт при ширине ≤900px и для ролей с `external:true`;
  - texts: убраны длинные пояснения (ws-sub, карточки бэкапа/миграций), `footNote` сокращён и берёт версию из `APP_VERSION` (была захардкожена v4.32.7).
  - Тесты: сценарии 27/27 (`docs/qa/tools/v4420_scenarios.js`), студия БЦ 8/8 (`v4420_studio_bc.js`), полный QA 34/34, роли 54/54.
- **v4.31.16–19:** унификация вида всех таблиц под «Базу брендов» (мелкий серый КАПС-заголовок, зебра, ховер); разгрузка панели ЛСР (4 кнопки + меню «⋯ Ещё»); **ЛСР по умолчанию компактный** — одна чистая строка заголовков, группы Бюджет/Факт свёрнуты, разворот кнопкой «▸ Бюджет/Факт детально».
- **v4.31.20:** закрыты дефекты внешнего аудита:
  - **P0-2** — нельзя скрыть `admin_modules`/`admin_system` (защита клиент+сервер, само-восстановление).
  - **P0-3** — `app_users` нельзя удалить через `data.php`; в `users.php` серверная проверка связей перед hard-delete.
  - **P1-4** — `tryPersist()` не кладёт тяжёлые данные (SVG/гео/PPT) в localStorage в backend-режиме.
- **v4.32.0–v4.32.7** (билд пользователя, влит в ветку): CASE Data Grid, единые таблицы, sticky-шапки LCR/SCR, AGX lockdown (`2026_07_23_v4321_agx_lockdown.sql`), API `unit_patch.php`/`units_batch.php`.
- **v4.33.0:** **закрыт P0-1** — у каждой версии планировки неизменяемый снапшот чертежа
  (`PLAN_CODES` + блоки/этажи + SVG по хешу в `PLAN_SNAPSHOT_SVGS` + контрольная сумма + автор/дата,
  deep-freeze при фиксации и при `applyState`); LCR генерируется строго из снапшота выбранной версии
  (выбор версии-источника на вкладке «Генерация LCR», баннер расхождения с текущим чертежом);
  handover фиксирует снапшот; «Создать из чертежа»/«Обновить площади» тоже проходят через версию
  (`planVersionForCurrent`); юниты штампуются `layoutVersionId`+`snapshotChecksum`.
  Ядро — `index.html` (блок `/* ===== P0-1 ... ===== */`) + `v417-master-plan.js`. Миграций БД нет.
- **v4.34.0 (UX-разгрузка):** десктопное меню по умолчанию закреплено с подписями (☰ сворачивает,
  настройка `navPin` в localStorage; правка скрывающего подписи правила — в `v3520-workspaces.js`);
  мини-тест показывается бейджем 🎓, а не окном поверх работы; из шапки убраны дубли «+/✎ Объект»;
  дашборд: один ряд кликабельных плиток «Центра действий», без служебных текстов/англо-меток;
  **исправлен `localize()`** — перевод хранится по-узлово (`data-ru-tN`), элементы со смешанным
  контентом («⏰ Внимание: <b>N</b>…») больше не калечатся; `v4327-patch.js` не откатывает номер
  версии в шапке. Скриншот-скрипт для визуальной ревизии — `docs/qa/tools/shots.js`.
- **v4.35.0 (закрыт весь остаток аудита + роли/безопасность):**
  P1-1 — защита юнита project-scoped (unitId → код+объект; чужой проект не влияет);
  P1-2 — массовый override удалён, защищённые площади разрешаются по-юнитно с причиной (история+аудит);
  P1-3 — `plan_master` единственный вход в планировки в меню, `project_layouts`/`leasing_layouts` — внутренние;
  P1-5 — validViews дополнен + динамическая досыпка из каталога, `analytics_hub` добавлен в lib.php;
  P1-6 — клиент шлёт только изменённые домены, сервер сливает по-ключево (`array_replace` в state.php),
  при 409 авто-слияние и повторное сохранение вместо «обновите страницу»;
  P2 — CSP+HSTS в `os/.htaccess`;
  Безопасность: `geo_collector.php`/`geo_seed_convert.php` были открыты из веба — теперь CLI-only (403);
  правило «только бренды» (BRJ) восстановлено в групповом меню v3520 (ОТКАЧЕНО в v4.41.2 — перекрывало настраиваемый workspace роли, у BRJ по умолчанию есть гео).
  Тесты: VM 19/19, браузерный QA 34/34, ролевой QA (все 10 демо-ролей) 54/54,
  статический аудит безопасности 0 crit/0 warn — всё в `docs/qa/` + инструменты в `docs/qa/tools/`.
- **v4.36.0:** платформа влита в `main` (страховка); инструкция автобэкапа+cron —
  `docs/ops/АВТОБЭКАП_И_CRON_ПОШАГОВО.md` (пользователь должен настроить на DirectAdmin!);
  мобайл: плитки дашборда 2 колонки, ряды табов — свайп одной строкой.
- **v4.36.1:** в баннере «Планировок» устаревшая подсказка про убранную из шапки кнопку
  «✎ Объект» заменена живой кнопкой «✎ Карточка объекта» (замечено на живом сайте).
- **v4.41.0 (аккордеон-фикс + гео-категории):** меню больше не сворачивается само (удалён
  клик-вне-меню обработчик; перерендер того же экрана меню не трогает; заголовок группы —
  независимый toggle; авто-раскрытие только при реальном переходе); гео-студия: «кластеры точек»
  вкл/выкл у каждой из 23 POI-категорий (⚙, персист в asaas_geo_layer_style), Аптеки получили
  размер/прозр./подписи, Медицина — прозрачность; матрица контролов 23/23+4/4 проверена
  автоматически. Тестировать студию локально: мокнуть /api/auth.php ({auth:true,user:{}}) и
  /api/workspace_access.php ({allowed:true}) в статик-сервере — гейт открывается.
- **v4.40.0 (студии и модалки):** финмодель ребрендирована «ASAAS OS» → «CASE OS» (шапка внутри
  студии); гео-студия — null-гарды в gate-режиме (без ошибок консоли автономно); MEP/Лифты
  осмотрены — чисто; модалки case49 осмотрены — единообразны; «Relationship owner» →
  «Ответственный менеджер». Автономно студии показывают fail-closed гейт — это норма.
- **v4.39.0 (обход всех 89 экранов):** новый инструмент `docs/qa/tools/all_views_sweep.js`
  (JS-ошибки/пустоты/многословие по каждому view); из меню убраны 38 future-заглушек
  (фильтр `!m.future` в buildNav v3520; каталог и админка workspaces не тронуты) — осталось
  41 работающий пункт в 7 группах; сокращены абзацы в «Рейтинге», «Системе», гео-хабе.
  ВНИМАНИЕ: при правке текстов regex'ами НЕ использовать жадные `[^<]{n,m}` — строки живут
  и в словарях ENMAP/UZMAP, и в JS-коде (дважды ломал скрипт, чинил из git).
- **v4.38.0 (единый скин):** таблицы модулей v32/v326/case49 и таббары `.geo-tabbar`
  приведены к эталону «Базы брендов»/`.tabs` приоритетными `#main`-правилами в ядре
  (CSS модулей не трогался); DOM-проверкой подтверждена идентичность computed-стилей;
  ещё одна устаревшая подсказка про «✎ Объект в шапке» (пустое состояние SCR) исправлена.
- **v4.37.0 (порядок в разделах, по жалобе «каша»):** меню-аккордеон — открыта только группа
  текущего экрана, группа следует за навигацией (обёртка `go()` в v3520, `caseNavGo`);
  дубли убраны: `plans` скрыт из меню (таб в LCR), `plan_master` переименован в «Версии планировок»,
  «Карта объектов» vs «Карта проектов Advisory», «Гео: наши проекты»/«Гео: рынок и POI»,
  укорочены длинные названия; многословные абзацы в v417 срезаны до одной строки.

## 7. Что осталось (из аудита v4.31.18)

**Весь аудит закрыт** (v4.31.20 → P0-2/P0-3/P1-4; v4.33.0 → P0-1; v4.35.0 → P1-1/P1-2/P1-3/P1-5/P1-6/P2).
Следующие шаги — не из аудита: staging-приёмка на реальном Apache/PHP/MySQL по чек-листу
(разделы A–J), проверка CSP на живом сайте, и продолжение UX-чистки по живым скриншотам.

Историческая формулировка закрытых пунктов:
- ~~**P0-1**~~ — **закрыт в v4.33.0** (см. раздел 6).
- **P1-1:** коммерческая защита юнита должна быть project-scoped (ключ `projectId+unitId`, а не только код помещения).
- **P1-2:** убрать массовый override защищённых площадей (`overrideProtected`), заменить на per-unit workflow с причиной и audit.
- **P1-3:** объединить/перенаправить три параллельных маршрута планировок (`plans`, `project_layouts`/`leasing_layouts`, `plan_master`) в единый master.
- **P1-5:** единый registry модулей (клиент `CASE_OS_MODULES` ↔ `validViews` ↔ серверный список). Расхождения: `admin_modules`/`admin_system`/`v32_demand` не в `validViews`; `chat` не в каталоге; `analytics_hub` не на сервере.
- **P1-6:** сохранение — перейти к patch/доменным ревизиям + diff/merge вместо глобального revision + 409.
- **P2:** обновить хвостовые метки версий (footer/SW-комментарий `v4.17.0`, `?v=4.17.0` в 16 script-URL, `DEPLOY.md` v4.10.0); добавить CHANGELOG/checksum текущего релиза; убрать пустые `.sha256`; рассмотреть CSP/HSTS.

## 8. Ссылки на артефакты аудита

Пользователь присылал (в предыдущей сессии): `..._DEEP_AUDIT_RU.md`, `..._STAGING_ACCEPTANCE_CHECKLIST_RU.md`, `..._STATIC_TEST_RESULTS.json`, `..._MASTER_PLAN_VM_TEST.json`, `..._FEATURE_FLAG_VM_TEST.json`. Чек-лист приёмки (разделы A–J) — использовать для staging-проверки на реальном Apache/PHP/MySQL.

---

### Готовый промпт для новой сессии (скопировать целиком)

> Продолжаем проект **CASE OS**. Весь код — в ветке **`claude/new-session-2kq1ff`** репозитория **`caseadvisoryservicess/case-site`**; забери её (`git fetch origin claude/new-session-2kq1ff && git checkout claude/new-session-2kq1ff`) и прочитай **`HANDOFF_CASE_OS.md`** в корне — там полный контекст: архитектура, деплой на DirectAdmin (`caseadvisory.uz/os/`, обновление вручную, автодеплоя нет), правила (ветка/секреты/версии), тестирование (`docs/qa/tools/verify_full_qa.js` + `docs/qa/tools/p01_vm_test.js`, `?demo=1`) и список оставшихся задач из внешнего аудита. Текущая версия — **v4.35.0** (весь внешний аудит закрыт: P0-1…P1-6, P2). Следующие шаги — staging-приёмка на реальном Apache/PHP/MySQL (чек-лист A–J), проверка CSP на живом сайте и UX-доработки по обратной связи пользователя. Каждый релиз: поднять `APP_VERSION` в `os/index.html` + кэш в `os/sw.js`, прогнать E2E, собрать `CASE_OS_vX_os_only.zip` (только `os/`, без `config.php`/секретов), запушить в ту же ветку и отдать мне ZIP. Не пушить в `main` и не создавать PR без моего явного разрешения.
