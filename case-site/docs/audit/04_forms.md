# Формы двух воронок CASE OS: аренда и Advisory

Карта того, что есть в коде. Только факты, пути и строки. Улучшения не предлагаются.

## Разбираемые файлы и сокращения

| Сокращение | Полный путь |
|---|---|
| `core` | `/home/user/case-site/case-site/os/core.js` (4181 стр.) |
| `v32` | `/home/user/case-site/case-site/os/v32-upgrade.js` (532 стр.) |
| `v35` | `/home/user/case-site/case-site/os/v35-stable.js` (488 стр.) |
| `v326` | `/home/user/case-site/case-site/os/v326-commission-engines.js` (314 стр.) |
| `v490` | `/home/user/case-site/case-site/os/v490-workflow.js` (284 стр.) |
| `v492` | `/home/user/case-site/case-site/os/v492-portfolio-proposals.js` (80 стр.) |

Воронка аренды/продажи: `core`, `v32`, `v35`, `v326`.
Воронка Advisory: `v490`, `v492`.

## Общие факты по обеим воронкам (проверено grep по всем шести файлам)

| Признак | Результат |
|---|---|
| HTML-атрибут `required` | 0 вхождений во всех шести файлах. Слово `required` встречается только как строка перевода (`v32:38,59,80`) и как значение опции `not_required` (`v32:473`) |
| Элемент `<form>` | 0 вхождений |
| `onsubmit` | 0 вхождений |
| `checkValidity()` / Constraint Validation API | 0 вхождений |
| `aria-invalid` | 0 вхождений |
| `beforeunload` | 1 вхождение - `core:957`, срабатывает только при незавершённой синхронизации состояния (`_localSaveFailed`, `_saveBusy`, `_savePend`, `_offlineDirty`, `_saveConflict`), а **не** при незакрытой форме с введёнными данными |

Следствие: во всех формах обеих воронок нет ни браузерной валидации, ни семантики формы. Вся проверка - ручная, в момент нажатия «Сохранить».

## Механизмы открытия форм: шесть разных контейнеров

| Механизм | Где определён | Закрытие по фону | Закрытие по Esc | Фокус на первое поле |
|---|---|---|---|---|
| Модалка `#case49Modal` (Advisory) | `v490:76-77` | да, `mousedown` по подложке (`v490:77`) | не найдено | да, `setTimeout(...,40)` (`v490:77`) |
| Модалка `.case492-modal-wrap` (Advisory, координаты) | `v492:38` (строится инлайн) | не найдено | не найдено | не найдено |
| Страница-маршрут (Advisory, конструктор КП) | `v492:52-60`, маршрут `advisory_proposal_builder` (`v492:73,74,78`) | - | - | не найдено |
| Выдвижная панель `#drawer` (аренда) | `core:3637 openDrawerUI`, `core:3638 closeDrawer` | не найдено (фон `#dbg` без обработчика) | да, `core:3639` | не найдено |
| Модалка `#rmodal` (аренда) | `core:3320-3321 openModal` | да, `m.onclick` (`core:3320`) | не найдено | не найдено |
| Модалка `.v32-modal` (аренда, v32) | `v32:172-179` | да, `m.addEventListener('click',…)` (`v32:176`) | не найдено | не найдено |
| Модалка `.v326-modal` (аренда, v326) | `v326:247-248` | не найдено | не найдено | не найдено |
| Выдвижная панель через `window.openDrawer` (аренда, v35) | `v35:428` (fallback - прямая запись в `#drawer`) | не найдено | наследуется от `core:3639` | не найдено |
| `prompt()` (аренда) | `core:3673,3712,3723,3732,3806,3842` | - | - | - |

---

# ЧАСТЬ 1. ВОРОНКА ADVISORY

## 1.1. Клиент - `case49ClientEdit` / `case49ClientSave`

- **Открытие:** модальное окно. `v490:95` (кнопки «+ Добавить клиента» `v490:90`, «Добавить первого клиента» `v490:91`, клик по имени в таблице `v490:88`).
- **Сохранение:** кнопка «Сохранить» → `case49ClientSave` (`v490:96`). Отмена: кнопка «Отмена» → `case49CloseModal` (`v490:95`), плюс клик по подложке.
- **Проверка:** только `name` - `if(!name){alert('Укажите название клиента.');return;}` (`v490:96`). Ошибка - системный `alert`, поле не подсвечивается, фокус не переводится.
- **После сохранения:** `save()` (`v490:45`: `audit` + `persist` + `apiSaveState` + `toast('Сохранено')`) → `closeModal()` → `go('crm_clients')` - полная перерисовка страницы клиентов независимо от того, откуда форма была открыта.

| Подпись | id | Элемент | Обяз. | По умолчанию | Ограничения |
|---|---|---|---|---|---|
| Название клиента | `c49_client_name` | input text | да (`v490:96`) | - | нет |
| Юридическое наименование | `c49_client_legal` | input text | нет | - | нет |
| Тип | `c49_client_type` | select | нет | «Собственник / девелопер» | 5 опций: Собственник / девелопер, Инвестор, Арендатор / бренд, Партнёр, Другое |
| Контактное лицо | `c49_client_contact` | input text | нет | - | нет |
| Телефон | `c49_client_phone` | input **tel** | нет | - | тип `tel`, формат не проверяется |
| Email | `c49_client_email` | input **email** | нет | - | тип `email`, формат не проверяется (нет `<form>`) |
| Ответственный менеджер | `c49_client_owner` | input text | нет | `currentUserName()` для новой записи (`v490:95`) | нет |
| Статус | `c49_client_status` | select | нет | «Активный» (`v490:95`) | 3 опции: Активный, Потенциальный, Архив |
| Примечание | `c49_client_notes` | textarea rows=4 | нет | - | нет |

Подсказок (`placeholder`) нет ни у одного поля: хелпер `field()` (`v490:78`) их не поддерживает.

## 1.2. Возможность (сделка) - `case49OpportunityEdit` / `case49OpportunitySave`

- **Открытие:** модалка, широкая (`wide`) - `v490:117`. Точки входа: «+ Новая возможность» (`v490:106`), «＋» в колонке Kanban с предзаполнением стадии (`v490:111`), клик по карточке (`v490:110`), клик по названию в таблице (`v490:112`).
- **Сохранение:** «Сохранить» → `case49OpportunitySave` (`v490:118`). Отмена - кнопка + подложка. Для существующей записи в футере есть третья кнопка «Создать КП» → `case49ProposalCreate` (`v490:117`), она уходит из формы, **не сохранив** введённые изменения.
- **Проверка:** `clientId` и `projectName` (`v490:118`), два последовательных `alert`.
- **После сохранения:** `save()` + `closeModal()` + `go('advisory_pipeline')`.

| Подпись | id | Элемент | Обяз. | По умолчанию | Ограничения |
|---|---|---|---|---|---|
| Клиент | `c49_opp_client` | select | да | пустая опция «Выберите клиента» | список `CASE_CLIENTS` |
| Название проекта / объекта | `c49_opp_project` | input text | да | - | нет |
| Связанный объект CASE | `c49_opp_object` | select | нет | «Пока не выбран» | `OBJECTS` кроме `archived` (`v490:60`) |
| Потенциальная услуга | `c49_opp_service` | input text | нет | - | свободный текст, **не** список `SERVICE_TYPES` |
| Этап воронки | `c49_opp_stage` | select | нет | `'new'` или переданная стадия | 12 стадий `PIPE_STAGES` (`v490:9-14`) |
| Ожидаемая стоимость | `c49_opp_value` | input number | нет | - | `min="0" step="0.01"` |
| Валюта | `c49_opp_currency` | select | нет | `USD` | USD, UZS, EUR |
| Вероятность, % | `c49_opp_probability` | input number | нет | `10` | `min="0" max="100"`; при сохранении `n()` не ограничивает |
| Ответственный | `c49_opp_owner` | input text | нет | `currentUserName()` | свободный текст |
| Источник лида | `c49_opp_source` | input text | нет | - | нет |
| Следующий шаг | `c49_opp_next` | input text | нет | - | нет |
| Дата следующего шага | `c49_opp_next_date` | input date | нет | `сегодня+2` | нет |
| Потребность клиента / заметки | `c49_opp_notes` | textarea rows=5 | нет | - | нет |
| Причина проигрыша | `c49_opp_lost` | input text | нет | - | **поле появляется только если `o.stage==='lost'` на момент открытия** (`v490:117`); при смене стадии на «Проиграно» в этой же форме поле не появится, а `case49OpportunitySave` всё равно читает `val('c49_opp_lost')` - пустая строка перезапишет прежнюю причину |

## 1.3. Перетаскивание карточки возможности - `case49OppDrop`

- **Открытие:** нет формы, drag&drop в Kanban (`v490:111,114,115`).
- **Сохранение:** мгновенное, без подтверждения (`v490:115`). Автоматически проставляет `nextAction`/`nextActionDate` при переходе в `proposal` (+3 дня) и `contract` (+2 дня), только если `nextAction` пуст.
- **Проверка:** нет. **Отмены нет.**
- **После:** `save()` + `go('advisory_pipeline')`.

## 1.4. Создание КП (модалка v490) - `case49ProposalCreate` / `case49ProposalSave`

**Важно:** `v492:78 install()` перехватывает `window.case49ProposalCreate` и подменяет его на переход в конструктор v492. Модалка `v490:123` остаётся доступной только через кнопку «Быстрое ручное КП» (`v492:60,71 case492QuickProposal`), которая вызывает сохранённый оригинал.

- **Открытие:** модалка широкая (`v490:123`).
- **Сохранение:** «Создать КП» → `case49ProposalSave` (`v490:124`). Отмена - кнопка + подложка.
- **Проверка:** **не найдено ни одной.** Можно создать КП с пустым номером, нулевой стоимостью и без связи с воронкой.
- **После:** `save()` + `closeModal()` + `case49ProposalOpen(p.id)` - открывается карточка КП. Если выбрана возможность, ей проставляется `stage='proposal'` и `proposalId`.

| Подпись | id | Элемент | Обяз. | По умолчанию | Ограничения |
|---|---|---|---|---|---|
| Возможность | `c49_prop_opp` | select | нет | «Без связи с воронкой» | `CASE_OPPORTUNITIES` |
| Номер КП | `c49_prop_no` | input text | нет | `CP-` + номер по количеству КП, `padStart(3,'0')` | уникальность не проверяется |
| Версия | `c49_prop_version` | input text | нет | `1.0` | при пустом значении подставляется `'1.0'` (`v490:124`) |
| Шаблон документа | `c49_prop_template` | select | нет | «Без шаблона» | `CASE_DOCUMENT_TEMPLATES` с `type==='proposal'` |
| Общая стоимость | `c49_prop_fee` | input number | нет | `o.value` возможности или пусто | `min="0" step="0.01"` |
| Валюта | `c49_prop_currency` | select | нет | валюта возможности или `USD` | USD, UZS, EUR |
| Действует до | `c49_prop_until` | input date | нет | `сегодня+14` | нет |
| Статус | `c49_prop_status` | select | нет | `draft` | 8 статусов `PROPOSAL_STATUSES` (`v490:16`) |
| Условия оплаты | `c49_prop_payment` | textarea rows=3 | нет | текст «Например: 50% аванс / 50% после передачи финального отчёта» **как значение, а не placeholder** | попадёт в запись, если не стереть |
| Исключения и примечания | `c49_prop_notes` | textarea rows=4 | нет | - | нет |

## 1.5. Карточка КП - `case49ProposalOpen`

- **Открытие:** модалка широкая (`v490:125-128`). Не форма - просмотр: полоса реквизитов, список Scope, блок коммерческих условий.
- **Действия в футере:** «Закрыть», «Изменить КП» (`case49ProposalEditMeta`), «Создать договор» (`case49ContractFromProposal`).

## 1.6. Изменение КП - `case49ProposalEditMeta` / `case49ProposalUpdate`

- **Открытие:** модалка широкая (`v490:129`).
- **Сохранение:** `case49ProposalUpdate` (`v490:130`). Отмена - кнопка + подложка.
- **Проверка:** **не найдено.**
- **После:** побочный эффект - при `status==='sent'` возможности ставится `stage='proposal_sent'`, при `accepted` → `'accepted'` (`v490:130`). Затем `save()` + `closeModal()` + `go('advisory_proposals')`.

Поля: Номер (`c49_prop_no`), Версия (`c49_prop_version`), Стоимость (`c49_prop_fee`, number, **без** `min`/`step` в отличие от формы создания), Валюта (`c49_prop_currency`), Действует до (`c49_prop_until`, date), Статус (`c49_prop_status`), Условия оплаты (`c49_prop_payment`, textarea 3), Примечания (`c49_prop_notes`, textarea 4).

Расхождение внутри самой воронки: поля `templateId` и `opportunityId` есть в форме создания, но отсутствуют в форме изменения - сменить шаблон или привязку к воронке после создания через UI нельзя.

## 1.7. Новая версия КП - `case49ProposalClone`

- **Открытие:** формы нет, кнопка «Новая версия» в строке таблицы (`v490:121`).
- **Сохранение:** мгновенное. Инкремент минорной версии, копирование записи и всех неархивных Scope-строк (`v490:131`).
- **Проверка и подтверждение:** **не найдено - нет `confirm`.**
- **После:** `save()` + `go('advisory_proposals')`.

## 1.8. Работа Scope - `case49ScopeItemEdit` / `case49ScopeItemSave`

- **Открытие:** модалка широкая из карточки КП (`v490:125,126,132`).
- **Сохранение:** «Сохранить». Отмена - кнопка + подложка. Для существующей записи есть «Исключить» → `case49ScopeArchive` (`v490:134`) с `confirm`.
- **Проверка:** только `title` (`v490:133`), `alert`.
- **После:** `save()` + `closeModal()` + `case49ProposalOpen(proposalId)` - возврат в карточку КП.

| Подпись | id | Элемент | Обяз. | По умолчанию | Ограничения |
|---|---|---|---|---|---|
| Название работы | `c49_scope_title` | input text | да | - | нет |
| Тип услуги / шаблон задач | `c49_scope_type` | select | нет | `generic` | 13 значений `SERVICE_TYPES` (`v490:20-25`) |
| Ответственное направление | `c49_scope_dept` | select | нет | `Advisory` | 6 значений `DEPARTMENTS` (`v490:19`) |
| Результат / deliverable | `c49_scope_deliv` | input text | нет | - | нет |
| Срок, рабочих дней | `c49_scope_days` | input number | нет | `5` | `min="1"` |
| Плановые часы | `c49_scope_hours` | input number | нет | `16` | `min="0" step="0.5"` |
| Цена работы | `c49_scope_price` | input number | нет | - | `min="0" step="0.01"` |
| Количество итераций | `c49_scope_iterations` | input number | нет | `2` | `min="0"` |
| Порядок | `c49_scope_order` | input number | нет | количество строк с этим `proposalId` + 1 (**считаются и архивные**, `v490:132`, тогда как вывод фильтрует `!s.archived` - `v490:125`) | `min="1"` |
| Предпочтительный исполнитель | `c49_scope_assignee` | select | нет | «Не назначено» | `people()` - `USERS` + `AGENTS` (`v490:61`) |
| Описание и границы работы | `c49_scope_desc` | textarea rows=4 | нет | - | нет |

## 1.9. Договор - `case49ContractFromProposal` + `case49ContractEdit` / `case49ContractSave`

- **Открытие:** формы создания нет. `case49ContractFromProposal` (`v490:138`) сразу создаёт запись `CASE_CONTRACTS` с авто-номером `CTR-NNN`, версией `1.0`, статусом `draft`, стоимостью/валютой/условиями оплаты из КП - и открывает модалку редактирования `case49ContractEdit` (`v490:139`). Если по этому КП уже есть договор не в статусе `terminated`, открывается существующий.
- **Сохранение:** «Сохранить» или «Сохранить и запустить проект» (кнопка меняется, если статус `signed`/`active` и проект ещё не создан) → `case49ContractSave(id, launch)` (`v490:140`). Отмена - кнопка + подложка; но запись договора уже создана до открытия формы, «Отмена» её не удаляет.
- **Проверка:** **не найдено ни одной.** Можно сохранить договор со статусом «Подписан» без даты подписания и с нулевой стоимостью.
- **После:** побочный эффект - при статусе `signed`/`active` возможности ставится `stage='won'`. Затем `save()` + `closeModal()` + либо `case49ContractLaunch(id)`, либо `go('advisory_contracts')`.

| Подпись | id | Элемент | Обяз. | По умолчанию | Ограничения |
|---|---|---|---|---|---|
| Номер договора | `c49_ctr_no` | input text | нет | `CTR-NNN` | нет |
| Версия | `c49_ctr_version` | input text | нет | `1.0` | нет |
| Статус | `c49_ctr_status` | select | нет | `draft` | 9 значений `CONTRACT_STATUSES` (`v490:17`) |
| Стоимость | `c49_ctr_value` | input number | нет | `totalFee` КП | без `min`/`step` |
| Валюта | `c49_ctr_currency` | select | нет | валюта КП | USD, UZS, EUR |
| Дата подписания | `c49_ctr_signed` | input date | нет | - | нет |
| Начало работ | `c49_ctr_start` | input date | нет | `todayISO()` | нет |
| Окончание по договору | `c49_ctr_end` | input date | нет | - | нет |
| Шаблон договора | `c49_ctr_template` | select | нет | «Без шаблона» | шаблоны с `type==='contract'` |
| График оплаты | `c49_ctr_payment` | textarea rows=4 | нет | `paymentTerms` договора или КП | нет |
| Примечания / условия | `c49_ctr_notes` | textarea rows=4 | нет | - | нет |

Ниже формы - информационный блок «Scope договора» с количеством работ, которые будут перенесены (`v490:139`).

## 1.10. Запуск проекта - `case49ContractLaunch`

- **Открытие:** формы нет - кнопка «Запустить проект» в таблице (`v490:137`) или «Сохранить и запустить проект» в договоре.
- **Действие:** создаёт `OBJECTS`-запись, если у возможности нет `objectId`; помечает `c.projectId`, `c.status='active'`; снимает `baselineSnapshot` со всех Scope-строк; `generateFromScope` (`v490:163`) генерирует задачи по шаблонам (`blueprint`, `v490:149-162`) и deliverables; возможности ставит `stage='won'`, `probability=100`; переключает `S.obj` и уходит на `go('project_workspace')` (`v490:164`).
- **Проверка и подтверждение:** **не найдено - нет `confirm`** перед необратимой генерацией задач.

## 1.11. Scope Change Request - `case49ScopeChangeEdit` / `case49ScopeChangeSave`

- **Открытие:** модалка широкая (`v490:179`). Требует выбранного проекта: `if(!pid){alert('Сначала выберите проект.');return;}`.
- **Сохранение:** «Отправить на согласование». Отмена - кнопка + подложка.
- **Проверка:** название работы **или** выбранная существующая работа (`v490:180`), `alert`.
- **После:** `save()` + `closeModal()` + `go('advisory_scope')`.
- **Согласование:** `case49ScopeChangeApprove` (`v490:181`) - `confirm` есть; требует право `canApprove()`.

Поля: Тип изменения (`c49_ch_type`, select: add/modify/remove, по умолчанию `modify` если Scope не пуст, иначе `add`), Связанная работа (`c49_ch_scope`, select, «Новая работа»), Название / новая формулировка (`c49_ch_title`), Тип услуги (`c49_ch_service`, select `SERVICE_TYPES`, по умолчанию `generic`), Направление (`c49_ch_dept`, select `DEPARTMENTS`, `Advisory`), Deliverable (`c49_ch_deliv`), Изменение срока, дней (`c49_ch_days`, number, `0`, **без min/max - отрицательные допускаются намеренно**), Изменение часов (`c49_ch_hours`, number, `0`), Изменение стоимости (`c49_ch_price`, number, `0`), Коммерческий режим (`c49_ch_commercial`, select: included/paid/internal/pending, по умолчанию `pending`), Причина и описание изменения (`c49_ch_reason`, textarea 5).

## 1.12. Задача - `case49TaskEdit` / `case49TaskSave`

- **Открытие:** модалка широкая (`v490:200`).
- **Сохранение:** «Сохранить» → `case49TaskSave` (`v490:201`). Отмена - кнопка + подложка.
- **Проверка:** только `title`, `alert` (`v490:201`).
- **После:** `save()` + `closeModal()` + `go(S.view==='work_kanban'?'work_kanban':'work_tasks')` - **если форма открыта с рабочего стола проекта (`v490:171`), после сохранения пользователя уносит на страницу задач.**

Поля: Название задачи (`c49_task_title`, обяз.), Проект (`c49_task_project`, select), Связанная работа Scope (`c49_task_scope`, select), Направление (`c49_task_dept`, select `DEPARTMENTS`, по умолчанию `Advisory`), Исполнитель (`c49_task_assignee`, select `people()`), Зависит от задачи (`c49_task_dep`, select), Статус (`c49_task_status`, select 9 значений `TASK_STATUSES` `v490:15`, по умолчанию `backlog`), Приоритет (`c49_task_priority`, select low/normal/high/critical, `normal`), Дата начала (`c49_task_start`, date, сегодня), Дедлайн (`c49_task_due`, date, сегодня+5), Плановые часы (`c49_task_plan`, number, `4`, `min="0" step="0.5"`), Фактические часы (`c49_task_actual`, number, `min="0" step="0.5"`), Ожидаемый результат / описание (`c49_task_desc`, textarea 4), Причина блокировки / комментарий (`c49_task_block`, textarea 3). Ниже - блок «История назначений» только для чтения.

Смена исполнителя внутри карточки записывается в `assignmentHistory` с причиной «Изменено из карточки задачи» без запроса причины (`v490:201`).

## 1.13. Инлайн-редактирование в таблице задач

- **Исполнитель:** `<select>` в ячейке (`v490:187`) → `case49TaskReassign` (`v490:196`) → открывается модалка с обязательным выбором причины (7 опций, по умолчанию «Решение руководителя») и комментарием → `case49TaskReassignSave` (`v490:197`). Отмена возвращает на `go(S.view)`, **но значение в селекте уже визуально изменено; перерисовка возвращает исходное**.
- **Статус:** `<select>` в ячейке (`v490:187`) → `case49TaskQuick` (`v490:194`) - **сохраняется сразу, без подтверждения и без отмены**, затем `go(S.view)`.
- **Чекбокс выбора строки:** `case49TaskSelect` (`v490:192`) → `go(S.view)` - **полная перерисовка таблицы на каждый клик по чекбоксу**.
- **Drag&drop в Kanban задач:** `case49TaskDrop` (`v490:199`) - сохранение сразу, отмены нет.

## 1.14. Массовое переназначение - `case49BulkAssign` / `case49BulkAssignSave`

- **Открытие:** модалка (`v490:202`), кнопка появляется при выбранных задачах (`v490:185`).
- **Проверка:** **не найдено** - можно применить с пустым исполнителем «Не назначено» (`v490:203`).
- **После:** `save()`, очистка выбора, `closeModal()`, `go(S.view)`.

Поля: Новый исполнитель (`c49_bulk_person`, select с «Не назначено»), Причина переназначения (`c49_bulk_reason`, select 6 опций, по умолчанию «Решение руководителя» - **список короче, чем в одиночном переназначении: нет «Задержка исполнения»**, ср. `v490:196` и `v490:202`), Комментарий (`c49_bulk_note`, textarea 3).

## 1.15. Массовая смена статуса - `case49BulkStatus` / `case49BulkStatusSave`

- **Открытие:** модалка (`v490:204`) - одно поле «Новый статус» (`c49_bulk_status`, select `TASK_STATUSES`, по умолчанию `doing`).
- **Проверка и подтверждение:** **не найдено.**
- **После:** `save()`, очистка выбора, `closeModal()`, `go(S.view)` (`v490:205`).

## 1.16. Deliverable - `case49DeliverableEdit` / `case49DeliverableSave`

- **Открытие:** модалка широкая (`v490:211`).
- **Проверка:** только `title`, `alert` (`v490:212`).
- **После:** `save()` + `closeModal()` + `go('advisory_reports')`.

Поля: Название (`c49_del_title`, обяз.), Проект (`c49_del_project`, select, «Выберите проект»), Тип (`c49_del_type`, select 6 опций), Статус (`c49_del_status`, select 9 значений `DELIVERABLE_STATUSES` `v490:18`, по умолчанию `planned`), Ответственный (`c49_del_owner`, select `people()`), Версия (`c49_del_version`, text, `1.0`), Срок (`c49_del_due`, date, сегодня+10), Дата передачи (`c49_del_sent`, date), Текущая итерация (`c49_del_iter`, number, `0`, `min="0"`), Лимит итераций (`c49_del_limit`, number, `2`, `min="0"`), Комментарий клиента / QA (`c49_del_notes`, textarea 5).

## 1.17. Версия планировки - `case49LayoutEdit` / `case49LayoutSave`

- **Открытие:** модалка широкая (`v490:221`); требует выбранного проекта (`alert('Выберите проект.')`).
- **Сохранение:** асинхронное (`v490:222`) - сначала загрузка файла `uploadProjectFile` (`v490:215`), затем `Object.assign`.
- **Проверка:** для новой не-исходной версии при наличии других версий обязателен «Основана на» (`v490:222`, `alert`). Версия, этаж, файл - не обязательны. Ошибка загрузки - `alert('Не удалось загрузить файл: '+…)`.
- **После:** `save()` + `closeModal()` + `go(S.view==='leasing_layouts'?'leasing_layouts':'project_layouts')`.

Поля: Тип планировки (`c49_layout_type`, select 5 значений `LAYOUT_TYPES` `v490:26`; по умолчанию `source` для первой версии, `leasing` для последующих), Версия (`c49_layout_version`, text; `A-00` для первой, `L-01` для последующих), Этаж / зона (`c49_layout_floor`, text, «Все этажи»), Основана на (`c49_layout_base`, select), Статус (`c49_layout_status`, select 5 опций, `draft`), Дата версии (`c49_layout_date`, date, сегодня), Что изменено и почему (`c49_layout_reason`, textarea 5), Файл планировки (`c49_layout_file`, input file, `accept=".pdf,.png,.jpg,.jpeg,.webp,.svg,.dwg,.dxf,.ifc,.rvt,.xlsx,.zip"`, подпись «до 25 МБ»; фактический лимит демо-режима - 2 МБ, `v490:215`).

Сопутствующие действия: «Сделать действующей» `case49LayoutActivate` (`v490:224`) - `confirm` есть; «В архив» `case49LayoutArchive` (`v490:225`) - **`confirm` не найден**, только проверка, что версия не действующая и не `source`.

## 1.18. Шаблон документа - `case49TemplateEdit` / `case49TemplateSave`

- **Открытие:** модалка широкая (`v490:237`). Формы редактирования существующего шаблона **не найдено** - только создание.
- **Проверка:** только `name` (`v490:238`), `alert`.
- **После:** `save()` + `closeModal()` + `go('document_templates')`.

Поля: Название (`c49_tpl_name`, обяз.), Тип (`c49_tpl_type`, select 6 опций, `proposal`), Язык (`c49_tpl_lang`, select RU/UZ/EN, `RU`), Услуга / направление (`c49_tpl_service`, text, значение по умолчанию `Advisory`), Версия (`c49_tpl_version`, text, `1.0`), Описание применения (`c49_tpl_notes`, textarea 4), Файл DOCX / PDF / XLSX (`c49_tpl_file`, file, `accept=".docx,.pdf,.xlsx,.doc,.xls"`).

## 1.19. Координаты проекта портфеля - `case492CoordinateEdit` / `case492CoordinateSave`

- **Открытие:** собственная модалка `.case492-modal-wrap`, строится инлайн (`v492:38`); ссылка «Уточнить» в таблице портфеля, видна только при `canEdit()` (`v492:34`).
- **Сохранение:** «Сохранить» → `case492CoordinateSave` (`v492:39`). Отмена - кнопка и крестик (оба `this.closest('.case492-modal-wrap').remove()`); **закрытия по подложке нет**.
- **Проверка:** диапазон координат - `if(lat<-90||lat>90||lng<-180||lng>180){alert('Проверьте координаты.');return;}` (`v492:39`). Единственная в обеих воронках проверка диапазона значения.
- **После:** `save()` (`v492:19` - `audit`+`persist`+`apiSaveState`, **без toast**), удаление модалки, `go(UI.route)`.

Поля: Широта (`c492_lat`, number, `step="0.000001"`), Долгота (`c492_lng`, number, `step="0.000001"`), Источник / комментарий (`c492_coord_src`, textarea, есть `placeholder`), Точная локация проверена (`c492_verified`, checkbox).

## 1.20. Конструктор КП (v492) - `renderProposalBuilder` / `case492ProposalSave`

- **Открытие:** **отдельная страница**, маршрут `advisory_proposal_builder` (`v492:52-60`, `73`, `74`, `78`). Единственная страничная форма в Advisory. Не модалка, вокруг неё нет подложки.
- **Сохранение:** кнопка «Создать КП» / «Сохранить изменения» (`v492:60`) → `case492ProposalSave` (`v492:67`). Кнопки «Отмена» нет - есть «Очистить» → `case492ProposalReset` (`v492:68`), которая **сбрасывает всё без подтверждения**.
- **Проверка** (`v492:67`, четыре разных механизма подряд):
  1. `if(!x.projectName){alert('Укажите проект.');return;}`
  2. `if(!x.selectedServices.length&&!x.customServices.length){alert('Выберите хотя бы одну услугу.');return;}`
  3. график платежей ≠ 100% → `confirm('График платежей не равен 100%. Сохранить?')` - **не блокирует**, только спрашивает
  4. `if(x.pricingMode==='manual_total'&&!x.overrideReason){alert('Для ручной итоговой цены укажите причину.');return;}`
  Номер КП, версия, срок действия, валюта, срок в днях - не проверяются.
- **После:** архивирует прежние Scope-строки этого КП с `source==='proposal_builder_v492'`, создаёт новые из выбранных услуг и кастомных работ; возможности ставит стадию по статусу КП (`accepted`→`accepted`, `sent`→`proposal_sent`, иначе `proposal`); чистит `UI.proposalId`, `UI.pendingCustom`, `UI.selectedServices`; `save()` + `go('advisory_proposals')`.

### Секция 1 «Клиент и проект» (`v492:55`)

| Подпись | id | Элемент | Обяз. | По умолчанию |
|---|---|---|---|---|
| Возможность | `c492_opp` | select | нет | «Без связи с воронкой»; `onchange="case492OppChange"` (`v492:63`) **перезаписывает поля «Клиент» и «Проект» без подтверждения** |
| Номер КП | `c492_no` | input text | нет | `CP-NNN` по количеству КП |
| Клиент | `c492_client` | input text | нет | имя клиента возможности |
| Проект | `c492_project` | input text | **да** | `projectName` возможности |
| Тип проекта | `c492_type` | select | нет | `retail`; 7 опций `TYPE_DEFAULTS` (`v492:16`); `onchange="case492TypeChange"` (`v492:62`) **перезаписывает модель цены и ставку за м²** |
| Локация | `c492_location` | input text | нет | - |
| Краткое понимание проекта | `c492_understanding` | textarea | нет | - |

### Секция 2 «Пакет и Scope» (`v492:56`)

- Переключатель «Ручной набор» (checkbox → `case492CustomMode`, `v492:61`).
- Три карточки-пакета (`v492:50`): `concept`, `feasibility` (по умолчанию), `strategic` (`v492:9-11`). Выбор пакета `case492SelectPackage` (`v492:61`) **перезаписывает `c492_rate` и `c492_days`**.
- Матрица из 31 услуги - чекбоксы `data-c492-service` (`v492:51`, каталог `SERVICES` `v492:14`).
- Строка добавления работы: `c492_custom_title` (text, есть placeholder «Добавить индивидуальную работу»), `c492_custom_price` (number, `min="0"`, placeholder «Цена»), кнопка «Добавить» → `case492AddCustomService` (`v492:65`): при пустом заголовке **молча ничего не делает** (`if(!title)return;` - без сообщения). Удаление строки `case492RemoveCustomService` - **без подтверждения**.

### Секция 3 «Цена и сроки» (`v492:57`)

| Подпись | id | Элемент | По умолчанию | Ограничения / видимость |
|---|---|---|---|---|
| Модель цены | `c492_price_mode` | select | из типа проекта | 5 опций: per_sqm, fixed, hybrid, stages, manual_total |
| Валюта | `c492_currency` | select | `USD` | USD, UZS, EUR |
| Расчётная площадь, м² | `c492_area` | number | `0` | `min="0" step="1"`; видно при per_sqm, hybrid (`v492:49`) |
| Ставка за м² | `c492_rate` | number | из `TYPE_DEFAULTS.rates` | `min="0" step="0.01"`; per_sqm, hybrid |
| Фиксированная часть | `c492_fixed` | number | `0` | `min="0" step="0.01"`; fixed, hybrid |
| Сумма этапов | `c492_stage_total` | number | `0` | `min="0" step="0.01"`; stages |
| Ручная итоговая цена | `c492_manual_total` | number | `0`/`totalFee` | `min="0" step="0.01"`; manual_total |
| Срок, рабочих дней | `c492_days` | number | `defaultDays` пакета (30/55/85) | `min="1"` |
| Причина ручной цены / скидки | `c492_override_reason` | textarea | - | placeholder «Обязательно при ручной итоговой цене…»; видно при manual_total; проверяется в `v492:67` |

Итог пересчитывается по `oninput` через `case492PriceUpdate` (`v492:62`), показывается в `c492_total` и в боковой карточке `c492_preview_total`.

### Секция 4 «Коммерческие условия» (`v492:58`)

Версия (`c492_version`, text, `1.0`), Действует до (`c492_until`, date, **сегодня+30** - против сегодня+14 в модалке `v490:123`), Аванс % (`c492_pay1`, number, `40`, `min="0" max="100"`), После чернового отчёта % (`c492_pay2`, `30`), После финального отчёта % (`c492_pay3`, `30`), Статус (`c492_status`, select - **6 значений: draft, internal_review, sent, revising, accepted, declined**, против 8 в `PROPOSAL_STATUSES` `v490:16`), Допущения, исключения и примечания (`c492_notes`, textarea).

Боковая панель (`v492:59`): предпросмотр + кнопки «Открыть документ» (`case492ProposalPreview`, `v492:70` - печатное окно) и «Скачать Word-совместимый файл» (`case492ProposalDownload`, `v492:70`). Обе работают с **несохранённым** `proposalPayload()`.

---

# ЧАСТЬ 2. ВОРОНКА АРЕНДЫ И ПРОДАЖИ

## 2.1. Объект - `addObjectForm` / `saveObject` (core)

- **Открытие:** выдвижная панель `#drawer` (`core:1588-1609`), только `R().admin`.
- **Сохранение:** «Создать»/«Сохранить» → `saveObject` (`core:1610`). Отмена - кнопка «Отмена» → `closeDrawer()`, плюс Esc (`core:3639`). Для существующего объекта справа кнопка «Удалить объект» → `deleteObject` (`core:1613`) с `confirm` **и** повторным вводом названия через `prompt`.
- **Проверка:** только название - `if(!name){alert('Введите название объекта');return;}` (`core:1610`). Плюс неявная нормализация: если сняты обе галочки модели сделки, принудительно включается «Аренда».
- **После:** `audit` + `persist` + `closeDrawer` + `rebuildObjSel` + `go(...)`. Для новой записи `S.obj` переключается на новый объект и уход на `v32_sales` или `registry` в зависимости от галочек.

Поля: Название (`o_name`, обяз.), Название (RU) (`o_ru`; при пустом = Название), Страна (`o_country`, «Узбекистан»), Город (`o_city`, «Ташкент»), Тип объекта (`o_type`, «Mixed-use»), Аренда (LCR) (`o_lease`, checkbox, включён), Продажа (SCR) (`o_sale`, checkbox, выключен), GBA м² (`o_gba`, `0`), GLA м² (`o_gla`, `0`), План (`o_plan`, select: Свой объект / Подписка ТЦ), Сервис-чардж % (`o_sc`, `10`), Индексация % (`o_inc`, `5`), Комиссия CASE % (`o_comm`, `8`), Этажность (`o_levels`, select single/dual), Широта (`o_lat`), Долгота (`o_lng`). Все числовые поля - обычные `text`-инпуты (`_bF`, `core:2941`), типа `number` и ограничений нет.

## 2.2. Помещение - `unitForm` / `unitSave` (core)

- **Открытие:** выдвижная панель `#drawer` (`core:3739-3760`), кнопка «Редактировать» в карточке помещения (`core:3633`). Новая запись создаётся отдельно - `addUnit` (`core:1734`) сразу пушит юнит с кодом `NEW-N` и **потом** через 60 мс открывает форму; отмена формы запись не удаляет.
- **Сохранение:** «Сохранить» → `unitSave` (`core:3761`). Отмена - кнопка «Отмена» → `openUnit(id)` (возврат в карточку), Esc → `closeDrawer`.
- **Проверка** (`core:3761-3764`): (1) `rateEditGuard` (`core:1629`) - `confirm`, если у помещения активное КП и ставка меняется; (2) уникальность кода внутри объекта - `alert`. Площадь, ставка, категория не обязательны.
- **После:** пересчёт `total`/`gap`, `commTrack`, `planUnignoreCode`, запись в `u.hist` и `audit`, `persist`, затем `CASE_LIVE_SYNC.entitySaved('unit',id)` или `refreshViewKeepScroll()`, и `openUnit(id)`.

Поля: Код помещения (`e_code`), Этаж (`e_floor`), Блок (`e_block`), Категория (`e_cat`, select `CASECATS`), Подкатегория (`e_sub`, input с `subDL`), Формат (`e_format`, datalist `fmtDL`), Концепт (`e_concept`, `conDL`), Зона (`e_zone`, `zoneDL`), Площадь м² (`e_area`), Терраса м² (`e_terr`), Ставка $/м² (`e_rate`), Статус аренды (`e_status`, select 7 значений), Статус продажи SCR (`e_salestatus`, select 4 значения), Бюджетная ставка (`e_budget`, по умолчанию `u.budget ?? u.rate`), Брокер (`e_broker`, select `brokerNames()`).

## 2.3. Инлайн-правка реестра, режим «по клику» - `editCell` / `saveCell` (core)

- **Открытие:** редактирование в строке (`core:1624`).
- **Сохранение:** для текстовых - `onblur` → `saveCell`; Enter = blur = сохранение; **Escape → `this.dataset.cancel=1` → `renderRegistry()`, то есть отмена есть** (`core:1627`). Для `status`/`broker` - `<select onchange>`, сохранение сразу, **отмены нет**.
- **Проверка:** `caseCanEditRegistry()` и `rateEditGuard` для ставки (`core:1631`).
- **После:** `u.hist.push`, `audit`, `persist`, `renderRegistry()` - полная перерисовка таблицы.

## 2.4. Инлайн-правка реестра, режим «редактирование строк» - `regEditRow` / `saveCellLive` (core)

- **Открытие:** режим включается кнопкой `toggleRegEdit` (`core:1632`), строка целиком превращается в набор инпутов и селектов (`core:1694-1711`).
- **Сохранение:** каждое поле - `onchange="saveCellLive(...)"` (`core:1634`). **Отмены нет. В историю юнита (`u.hist`) ничего не пишется - в отличие от `saveCell` (`core:1631`). `audit` тоже не вызывается.** Сохранение - через `tryPersist()` + `regLiveRefresh(u,field)`.
- **Проверка:** **не найдено.** Даже `rateEditGuard` здесь не вызывается, хотя в `saveCell` он есть - ставку можно молча изменить при активном КП.

Поля строки: код, блок, этаж, категория, подкатегория, площадь (number, `step="0.1" min="0"`), редактируемые бюджетные и фактические колонки (`lcrBudVis`/`lcrFacVis`), варианты брендов (input с `brandsDL` → `saveVarsLive`), брокер (select), комментарий (см. ниже), статус (select) и кнопка удаления `deleteUnit` (`confirm` есть, `core:1623`).

## 2.5. Комментарий в строке реестра - `regQuickComment` (core)

Единственная форма в обеих воронках с явной защитой от потери данных при перерисовке (описание проблемы - `core:1712-1719`):
- текст живёт в черновике `window.REG_CDRAFT` (`core:1720`), обновляется по `oninput` (`core:1709`);
- сохраняется **только по Enter**, не по blur;
- обёртка `renderRegistry` (`core:1725-1733`) восстанавливает фокус и позицию курсора после перерисовки.
- **Отмены нет** (комментарии только добавляются), проверка - только непустой текст.

## 2.6. Отказ - `addRefus` / `rfObjChange` / `saveRefus` (core)

- **Открытие:** модалка `#rmodal` (`core:2759-2776`); кнопка «+ Отказ» в отказном листе SCR (`v32:426`) и в реестре.
- **Сохранение:** «Сохранить» → `saveRefus` (`core:2782`). Отмена - кнопка + крестик + клик по подложке.
- **Проверка:** объект и бренд - два последовательных `alert` (`core:2788-2789`). Причина отказа **не обязательна**, хотя это главная ценность записи.
- **После:** `audit`, `persist`, удаление модалки, `refusRerender()` (`core:2794`) - `go('v32_sales')` или `renderRegistry()`.

Поля: Объект (`rf_obj`, select, `onchange="rfObjChange()"` перезаполняет список помещений), Помещение (`rf_unit`, select, «- не привязано к помещению -»), Бренд, который отказал (`rf_brand`, обяз.), Что / на каких условиях предлагали (`rf_cond`, textarea, есть placeholder-пример), Причина отказа (`rf_reason`, textarea).

## 2.7. Вариант → отказной лист - `varToRefusal` / `saveVarRefusal` (core)

- **Открытие:** модалка `#rmodal` (`core:3675-3684`).
- **Проверка:** **не найдено** - можно сохранить с пустой причиной (`core:3685`).
- **После:** запись в `REFUSALS`, удаление варианта из `u.vars`, `u.hist`, `audit`, `persist`, удаление модалки, `refreshViewKeepScroll()`, `openUnit(id)`.

Поля: Что / на каких условиях предлагали (`vr_cond`, textarea, **предзаполняется** строкой «площадь N м², ставка X $/м², подкатегория, категория» - `core:3676`), Причина отказа (`vr_reason`, textarea, placeholder с примерами).

## 2.8. Контрольная дата - `addUnitDate` / `saveUnitDate` (core)

- **Открытие:** модалка `#rmodal` (`core:3691-3701`).
- **Проверка:** тип даты и дата - два `alert` (`core:3707-3708`).
- **После:** `u.dates.push`, `u.hist`, `audit`, `persist`, удаление модалки, `refreshViewKeepScroll()`, `openUnit(id)`.

Поля: Тип даты (`ud_type`, select: Открытие, Окончание брони, Подписание контракта, Окончание договора, Ревизия ставки, Дедлайн, «Другое»), своё значение (`ud_custom`, показывается при «Другое»), Дата (`ud_date`, date, сегодня).

## 2.9. Формы на `prompt()` (core)

| Действие | Строка | Проверка | Отмена |
|---|---|---|---|
| `addVar` - добавить бренд-кандидата | `core:3673` | `if(v)` - пустая строка молча отменяет ввод | Cancel |
| `addComment` - комментарий по сделке | `core:3806` | `if(c)` | Cancel |
| `negotiateUnit` - перевести в переговоры | `core:3723-3731` | `if(!names.length){alert(...)}`; предзаполняется текущими вариантами | Cancel (`v==null`) |
| `editUnitArea` - уточнить фактическую площадь | `core:3732-3738` | `if(!(a>0)){alert('Введите число больше нуля.')}` | Cancel |
| `extendOffer` - продлить срок ответа по КП | `core:3712-3721` | `if(!(days>0)){alert('Введите число дней больше нуля.')}` | Cancel |
| `addDocContact` - контакт для документов | `core:3842` | ФИО обязательно (`if(!name)return;`), остальные три `prompt` без проверки | Cancel на первом |
| `deleteObject` - подтверждение названием | `core:1613` | сравнение с `o.name` | Cancel |

## 2.10. Объединение / разделение / перенос площади (core)

- `mergeUnitForm` (`core:3642-3644`) → `doMerge` (`core:3645`): выдвижная панель, одно поле `mg_to` (select). Проверки перед `doMerge` **не найдено** - только `if(!o||o.id===u.id)return;`. **Нет `confirm`**, хотя присоединяемое помещение удаляется из реестра.
- `splitUnitForm` (`core:3646`) → `doSplit` (`core:3649`).
- `areaTransferForm` (`core:3651`) → `atInfo` (`core:3665`) → `doTransferArea` (`core:3666`).

## 2.11. Бренд - `brandForm` / `brandSave` (core)

- **Открытие:** выдвижная панель `#drawer` (`core:2945-2980`). `brandEdit(id)` (`core:2936`) при отсутствии `id` **сразу создаёт запись** `BRANDS` с названием «Новый бренд» и категорией «Еда и напитки», и только потом открывает форму - отмена запись не удаляет.
- **Сохранение:** «Сохранить» → `brandSave` (`core:2982`). Отмена → `openBrand(id)`. Справа «Удалить бренд» → `deleteBrand` (`core:2981`) с `confirm`.
- **Проверка:** **обязательных полей не найдено.** `b.name=g('e_name')||b.name` (`core:2983`) - пустое название молча оставляет прежнее. Единственная проверка - поиск дубля по нормализованному названию (`core:2990`) → `confirm` с предложением объединить (`brandMerge`, `core:2996`).
- **После:** `persist()` + `CASE_LIVE_SYNC.entitySaved('brand',id)` или `refreshViewKeepScroll()` + `openBrand(id)`.

Поля (`core:2951-2977`): три блока изображений (`imgBox` - логотип, витрина, интерьер), Название (`e_name`), Категория (`e_cat`, select), Подкатегория (`e_sub`, datalist `subDL`), Тип (`e_type`, datalist `typeDL`), Алкоголь (`e_alco`, checkbox - **блок показывается только если `catHasAlcohol(b.cat)` на момент открытия**), Страна (`e_country`), Статус (`e_status`, select `BRANDST`), Ответственный (`e_owner`), Последний контакт (`e_lastc`, **обычный text с подписью «ГГГГ-ММ-ДД», не `type=date`**), Ценовой уровень (`e_price`, select A-D), Уровень бренда (`e_tier`, select A-D), Площадь min/max (`e_amin`, `e_amax`), Формат (`e_format`), Франшиза / модель (`e_fr`), Требования к помещению (`e_reqs`), Co-tenancy (`e_coten`), Контактные лица (`e_contacts`, **одна textarea, строки формата `Имя | Должность | Телефон | Email`**, `core:2966`, парсинг `core:2986`), Сайт (`e_site`), Instagram (`e_ig`), Год основания (`e_founded`), Группа / холдинг (`e_group`), Ценовой уровень - описание (`e_price_desc`, **записывается в то же поле `b.price`, что и select `e_price`**, `core:2983` - второй перезаписывает первый), Формат ICSC (`e_icsc`), Сеть: точек (`e_netPts`), Сеть: стран (`e_netCountries`), Оператор в Узбекистане (`e_uzOp`), О бренде (`e_about`), Позиционирование (`e_pos`), Концепция и ассортимент (`e_concept`), Рекомендация CASE (`e_rec`), Заметки (`e_notes`).

## 2.12. Бренд - `brandFormStable` / `asaas35SaveBrand` (v35)

Вторая, параллельная форма того же бренда.

- **Открытие:** выдвижная панель через `window.openDrawer` (`v35:410`, `428`). Для нового бренда данные берутся из черновика localStorage (`v35:409`, `draftLoad` `v35:364`).
- **Сохранение:** «Сохранить бренд» → `asaas35SaveBrand` (`v35:446`). Рядом «Закрыть» и, для нового, «Очистить черновик» (инлайн `localStorage.removeItem` + `closeDrawer` + `toast`, **без подтверждения**), для существующего - «🗑 Удалить бренд» → `asaas35DeleteBrand` (`v35:429`) с `confirm` и проверкой прав.
- **Черновик:** `draftSaveFromDrawer` (`v35:365`) вешается на все поля (`wireDraftForm`, `v35:408`) и вызывается при закрытии панели (`patchCloseDrawer`, `v35:448`). **Работает только для новой записи:** `if(!dr||dr.dataset.brandDraft!=='new')return;` - при редактировании существующего бренда черновик не сохраняется.
- **Проверка:** только название - фокус + `n.style.borderColor='var(--red)'` + `toast('Введите название бренда')` (`v35:446`). Это единственное место в обеих воронках, где поле и подсвечивается, и получает фокус, и сопровождается тостом.
- **После:** `draftClear`, `persistSafe`, `closeDrawer`, затем `CASE_LIVE_SYNC.entitySaved('brand',b.id)` или `renderBrandsResultsOnly()`, `toast('Бренд сохранён')`.

Отличия набора полей от `core:2945` (та же сущность):
- **есть в v35, нет в core:** «Компания / группа / владелец бренда или франшизы» (`bd_group`, читается из пяти возможных полей - `brandCompanyValue`, `v35:380`), «Присутствие в Узбекистане» (`bd_presence`, select 5 опций), «Варианты площади / исходное значение» (`bd_areaVariants`, textarea), «Проверка данных» (`bd_reviewFlag`, textarea), «Последний контакт» как `type="date"` (`v35:418`);
- **есть в core, нет в v35:** блоки изображений (`imgBox`), отдельное поле «Ценовой уровень (описание для профиля)»;
- **контакты:** в v35 - повторяющиеся строки-блоки с полями ФИО / Должность / Код страны / Номер телефона / Email (`contactRowHtml`, `v35:401-405`), добавление `asaas35AddContact` (`v35:406`), удаление `asaas35RemoveContact` (`v35:407`, **без подтверждения**), нормализация номера `normPhoneWithCC` (`v35:446`). В core - одна textarea со строками через `|`.

## 2.13. Запрос бренда (лид аренды) - `v32RequestForm` / `v32SaveRequest`

- **Открытие:** модалка `.v32-modal` (`v32:467`, `172`).
- **Сохранение:** «Сохранить» → `v32SaveRequest` (`v32:468`). Отмена - кнопка + крестик + подложка.
- **Проверка:** только `brand` - `if(!row.brand){toast(tr('needBrand'),'warn');return;}` (`v32:468`). Поле не подсвечивается, фокус не переводится.
- **После:** `persistV32()` (`v32:193` - только `persist()`), `v32CloseModal()`, `go('v32_requests')`. **Тоста об успешном сохранении нет.**

Поля: Бренд (`rq_brand`, обяз.), Источник (`rq_source`, `inbound`), Страна (`rq_country`, «Узбекистан»), Город (`rq_city`), Категория (`rq_cat`), Площадь min (`rq_amin`, number), Площадь max (`rq_amax`, number), Бюджет (`rq_budget`, number), Этап (`rq_stage`, select: **new, matching, offer, won, lost**), Брокер (`rq_broker`, `brokerName()`), Комиссия (`rq_fee`, number), Следующий шаг (`rq_next`), Срок (`rq_due`, date, сегодня+3), Предпочтения (`rq_pref`, textarea).

Хелпер `formInput` (`v32:461`) не принимает атрибутов: **ни одно поле форм v32 не имеет `placeholder`, `min`, `max`, `step`.**

Сопутствующее: `v32MatchRequest` (`v32:469`) - модалка подбора помещений, «Шорт-лист» → `v32RequestToUnit` (`v32:471`), сохраняет сразу, **без подтверждения и отмены**.

## 2.14. Инвестор-лид - `v32InvestorForm` / `v32SaveInvestor`

- **Открытие:** модалка `.v32-modal` (`v32:473`).
- **Проверка:** только `investor` - `toast(tr('investor')+' - '+tr('required'),'warn')` (`v32:474`).
- **После:** `persistV32`, `v32CloseModal`, `go('v32_investors')`. Тоста об успехе нет.

Поля: Инвестор (обяз.), Источник (`inbound`), Страна («Узбекистан»), Город, Тип актива (`ir_type`, «Street retail / leased unit»), Бюджет min/max, Площадь min/max, Cap rate (`ir_cap`, **text, не number**), Подтверждение средств (`ir_pof`, select: pending/confirmed/not_required), Контакт, Этап (`ir_stage`, select: **new, matching, nda, dd, offer, spa, won, lost**), Брокер, Комиссия, Следующий шаг, Срок (date, сегодня+4), Инвестиционные критерии (textarea).

Сопутствующее: `v32MatchInvestor` (`v32:475`) → `v32InvestorToAsset` (`v32:476`) - сохраняет сразу; `v32InvestorToBuyer` (`v32:477`) - создаёт покупателя, **без подтверждения**, дубли ловит по названию в нижнем регистре.

## 2.15. Актив на продажу - `v32AssetForm` / `v32SaveAsset`

- **Открытие:** модалка `.v32-modal` (`v32:479`).
- **Проверка:** только `name` (`v32:480`), `toast … 'warn'`.
- **После:** `persistV32`, `v32CloseModal`, `go('v32_sales')`.

Поля: Объект (`sa_name`, обяз.), Type (`sa_type`, «Investment»), Страна («Узбекистан»), Город, Площадь (`sa_area`, number), Цена (`sa_price`, number), % комиссии (`sa_cr`, number, `2`), Комиссия (`sa_comm`, number; при пустом считается `price*cr/100`), Этап (`sa_stage`, select: **lead, nda, dd, offer, spa, closed, lost**), Брокер, Доступ (`sa_vis`, select internal/partners), Продавец (`sa_seller`), Следующий шаг, Срок (date, сегодня+7), Notes (textarea). **Валюта не поле - жёстко `currency:'USD'` в `v32:480`.**

## 2.16. Покупатель - `v32BuyerForm` / `v32SaveBuyer`

- **Открытие:** модалка `.v32-modal` (`v32:481`). **Проверка:** только `name` (`v32:482`).
- Поля: Покупатель (обяз.), Asset type («Retail»), Страна, Город, Брокер, Budget min/max, Площадь min/max, Cap rate, Email / Телефон (`sb_contact`, **одно поле на два канала**), Notes. Статус жёстко `stage:'active'` (`v32:482`).

## 2.17. Партнёр - `v32PartnerForm` / `v32SavePartner`

- **Открытие:** модалка `.v32-modal` (`v32:485`), только `canManage()` (`v32:456`). **Проверка:** только `name` (`v32:486`).
- Поля: Партнёр (обяз.), Type («Partner»), Страна, Город, Ответственный, Email, Телефон, Доступ (`pt_access`, select: sales_only / requests_only / requests_and_sales / readonly), Область (`pt_scope`, «assigned_city»), Статус (`pt_status`, select pending/active/paused), Notes.

## 2.18. Инлайн-редактирование в реестре продаж (SCR) - v32

- **Статус продажи:** `<select>` прямо в ячейке (`v32:407`, `scrStatusCell`) → `scrSetSaleStatus` (`v32:452`). Сохранение мгновенное, `persist`, `audit`, `refreshViewKeepScroll`. **Отмены нет.**
- **Цена продажи:** `<input type="number" min="0" step="1">` в ячейке (`v32:408`) → `scrSetSalePrice` (`v32:453`) по `onchange`. Значение приводится к `Math.max(0, …)`. **Отмены нет, `audit` не вызывается** (в отличие от смены статуса).
- Оба контрола вызывают `event.stopPropagation()`, чтобы клик не открыл карточку.

## 2.19. Документ «Коммерческое предложение» - `pickT('kp')` / `genKP` (core)

Самая большая форма обеих воронок.

- **Открытие:** **страница**, маршрут `docs`, контейнер `#dform` (`core:3974 renderDocs`, `core:3981 pickT`). Вкладки kp / loi / remind (`core:3983`). Из реестра массово - `regBulkKP` (`core:1735`): переход на `docs` и заполнение полей через `setTimeout(...,90)`.
- **Сохранение:** кнопка внизу формы → `genKP` (`core:4050`). Отдельной кнопки «Отмена» **нет** - уход со страницы через навигацию.
- **Проверка:**
  1. в самом `genKP` - только арендатор: `if(!$('f_ten').value.trim()){alert(…);return;}` (`core:4053`) и `ensureBrandInBase(ten)` (`core:4054`);
  2. остальное добавляет модуль v32 обёрткой (`v32:504-505`): `genKP`/`genLOI`/`genREM` оборачиваются в `validateDoc()` (`v32:499`). Проверяются: бренд `f_ten`; для КП - помещение `f_unit`, площадь `f_area` > 0, ставка `f_rate` > 0 (`v32:494`); для LOI - то же, но только при `f_loitype==='unit'` (`v32:495`); для напоминания - тема `f_sub` и дата `f_date2` (`v32:496`). **Если модуль v32 не загружен, этих проверок нет.**
  3. **Показ ошибок здесь единственный полноценный в обеих воронках** (`v32:489-503`): класс `case-invalid` на поле, блок `.case-field-error` под полем, сводный блок `#v32DocErrors` в начале формы со ссылками-переходами (`v32JumpField` → `scrollIntoView` + `focus` + `select`), плюс живой чеклист готовности в процентах `.v32-checklist` (`v32:502-503`), пересчитываемый по `input`/`change` (`v32:512-513`).
  4. Ненайденный в базе бренд: `ensureBrandInBase` подменяется в `v32:506-510` - вместо блокировки показывает под полем кнопку «+ Добавить бренд» (`v32AddBrandFromDoc`, `v32:511`).
- **Черновик:** `saveDocDraft` (`v32:515`) в `localStorage['caseos-v32-docdraft-'+DOC.t]` по каждому `input`/`change`; `restoreDocDraft` (`v32:516`) при отрисовке формы; очистка после выпуска документа (`v32:517`, обёртка `genEmit`).
- **После:** `genEmit` (`core:3850-3853`) - запись в `DOCREG` либо новая версия существующей записи с историей `versions[]`; `audit`; `persist`; перерисовка блока истории `#dochist`. Побочные эффекты по помещениям (`core:4062+`): предупреждение `confirm`, если помещение уже предложено другим агентом; проставление `u.rate`, `u.total`, `u.gap`; запись `u.offers` и `u.offer`; статус помещения → `'off'`, если он не из числа занятых.

Поля формы КП (`core:3988-4005`), по секциям:
1. Объект (`f_obj`, select `visibleObjects()`, `onchange="kCfg()"`); № КП (`f_no`, значение `CA/0001`); Дата (`f_date`, текст с сегодняшней датой в `ru-RU`); Срок действия КП, дней (`f_valid`, number, `10`); Кому - ФИО (`f_addr`, placeholder «напр. Хайдаров А.»).
2. Арендатор / торговое название (`f_ten`, input + `list="brandDL"`, placeholder, обязателен); Помещение(я) (`f_unitSel`, **multiple select size=5** → `unitSelMulti()`, плюс ручной ввод `f_unit`); Деятельность (`f_act`, select `ACTS` + ручной ввод `f_actm`, показываемый при «Другое»).
3. Площадь (`f_area`, number, `sel.area` или `100`, `oninput="pricePrev()"`); Ставка (`f_rate`, number, `sel.rate` или `20`).
4. Блок «Бюджет владельца → предложение»: Бюджетная ставка (`f_budget`, number), Корректировка (`f_adjtype`, select **7 опций**: без изменений, наценка %, скидка %, надбавка $/м², скидка $/м², надбавка к сумме/мес, скидка с суммы/мес), Значение корректировки (`f_adjval`, number, `0`), живой предпросмотр `#f_pricePrev` через `applyPricing()`.
5. Блок «Терраса»: чекбокс `f_terr_on`, Площадь террасы (`f_terr`), Ставка террасы (`f_terrate`, по умолчанию половина ставки), Сезонная (`f_season`, checkbox), Месяцы (`f_seasonm`, placeholder).
6. Блок «Модель аренды»: `f_rmodel` (select `RENTMODELS`, `onchange="rmodel()"`), График (`f_rstep`, скрыт), % от оборота (`f_turn`, `8`) и Порог (`f_turnmin`) - в скрытом блоке, Повышение %/год (`f_inc`, `5`), Обслуживание % (`f_sc`, `10`).
7. Блок «НДС»: Режим (`f_vat`, select `VATOPT`), Ставка НДС % (`f_vatr`, `12`).
8. Блок «CAPEX и каникулы»: CAPEX - кто (`f_capx`, select Арендатор/Арендодатель/Совместно), CAPEX - сумма/условие (`f_capxv`), Каникулы мес (`f_fit`, `2`), Период каникул (`f_fitp`), Предоплата мес (`f_prep`, `1`), Депозит мес (`f_dep`, `1`).
9. Блок «Коммунальные платежи» `#umx` (`core:4007`): для каждого вида из `UTILS` - чекбокс `u_*` (по умолчанию отмечен), select метода `um_*` (`UMETHODS`), поле суммы `ua_*` (скрыто до выбора метода, `umMethod`). Предустановки - `core:4008`.
10. Состояние при передаче (`f_condSel`, select `CONDOPTS` + ручной ввод `f_cond` со значением «В черновом виде (shell & core)»); Особые условия (`f_special`, textarea, placeholder).
11. Подписант (`f_sigSel`, select `sigRoster()` + «Другое» → `f_sig` со значением «Шермухамедов А.А., Директор»).
12. `docExtraFields()` (`core:3837-3841`): Контактное лицо (`f_contact`, select + кнопка «+» → `addDocContact`), Вступительный текст (`f_introSel` select + `f_intro` textarea), Благодарность (`f_thanksSel` + `f_thanks`).

Ни одно числовое поле формы КП не имеет `min`/`max`/`step`.

## 2.20. Документ LOI - `pickT('loi')` / `genLOI` (core)

- **Открытие:** та же страница `#dform`, вкладка LOI (`core:4012`).
- **Поля:** Тип LOI (`f_loitype`, select: по помещению / общее по бренду / общее по объекту, `onchange="loiType()"`), Объект (`f_obj`), Арендатор / бренд (`f_ten` + `brandDL`), блок «по помещению» (`f_unit`, `f_area`, `f_rate`) - показывается по типу, Срок действия LOI, дней (`f_val`, number, `14`), плюс `docExtraFields()`.
- **Проверка:** только через обёртку v32 и только для типа `unit` (`v32:495`). Для типов «общее по бренду» и «общее по объекту» проверяется лишь наличие бренда.
- **После:** `genEmit` (`core:4118`) - запись в `DOCREG` с `no:'-'`, то есть **без номера, а значит без версионирования** (ключ версий строится только при `meta.no!=='-'`, `core:3850`). Статусов помещения LOI не меняет.

## 2.21. Документ «Напоминание» - `pickT('remind')` / `genREM` (core)

- **Поля** (`core:4015`): Объект (`f_obj`), Арендатор (`f_ten`), Тема (`f_sub`, по умолчанию «Окончание срока договора»), Дата события (`f_date2`, date, **жёстко `2026-09-01` в разметке**), плюс `docExtraFields()`.
- **Проверка:** тема и дата - только через обёртку v32 (`v32:496`).
- **Вход из карточки помещения:** `remindFromUnit` (`core:3640`) - предзаполнение через `setTimeout(...,90)`.

## 2.22. Статус выпущенного документа - `setDocStatus` (core:3886)

Селект в истории документов, сохранение мгновенное, `persist()`, перерисовка `#dochist`. Проверок и отмены нет.

## 2.23. Комиссионная сделка по аренде - `v326LeaseDealForm` / `v326SaveLeaseDeal`

- **Открытие:** модалка `.v326-modal` (`v326:232-246`, `247`). Закрытия по подложке нет - только крестик и «Отмена».
- **Живой предпросмотр:** контейнер формы имеет `oninput`/`onchange="v326LeasePreview()"` (`v326:235`), расчёт `leaseCalc` показывается в `#v326LeasePreview` (`v326:258`) - таблица участников, ролей, долей и сумм.
- **Проверка** (`v326:259`): (1) `tenant` обязателен; (2) при `paymentStatus==='paid'` и `cfg.needsAshConfirmation` требуется подтверждение ASH. Ошибка через `fieldWarn` (`v326:260`) - класс `v326-invalid` + `focus()` + `alert()`. Числовые доли и суммы не проверяются.
- **Право на подтверждение:** чекбокс `lc_ash` рендерится только при `rights().approve` (`v326:240`); в `formDealFromDOM` (`v326:254-255`) для остальных значение берётся из существующей записи, а не из DOM.
- **После:** `audit326`, `persist326`, `v326CloseModal()`, `go('v326_lease')`. Тоста нет.

Поля: Объект (`lc_obj`, select по названиям `OBJECTS`), Помещение (`lc_unit`, text - **не связано с реестром `U`**), Арендатор (`lc_tenant`, обяз.), Площадь (`lc_area`, number), Ставка (`lc_rate`, number), Срок мес (`lc_term`, number, `12`), Дата подписания (`lc_signed`, date, сегодня), Метод базы (`lc_base_method`, select: manual / one_month / annual_percent / fixed), База комиссии (`lc_C`, number), % annual (`lc_annpct`, number), Валюта (`lc_cur`, select **USD / UZS**), Сценарий (`lc_scenario`, select: curator_self / external / internal), Этап (`lc_stage`, select **8 значений**: lead, neg, loi, contract, signed, paid, lost, reversed - `v326:195`), Статус оплаты (`lc_pay`, select: pending / paid / lost / reversed), Подтверждение ASH (`lc_ash`, checkbox, условный), Основной агент (`lc_a1`, select `brokers()`), Второй агент (`lc_a2`, select с «-»), % A1 (`lc_split1`), % A2 (`lc_split2`), Куратор (`lc_curator`, select), Поддержка (`lc_support`, select), Оклад (`lc_salary`, number), Notes (textarea). Ни одного `min`/`max`/`step`.

## 2.24. Настройки комиссии по аренде - `v326LeaseSettings` / `v326SaveLeaseSettings`

- **Открытие:** модалка `.v326-modal` (`v326:262-266`). **Проверок нет** (`v326:267`); суммы долей не сверяются со 100%. Единственная страховка - `window.COMMISSION_ENGINE_CFG.fxRate=num(val('ls_fx'))||11974`.
- Поля: метод базы (`ls_base`), % annual (`ls_ann`), фикс (`ls_fixed`), доли `ls_cs`/`ls_ex`/`ls_in`/`ls_ho`/`ls_curshare`/`ls_comp3`, оклад (`ls_sal`), курс (`ls_fx`), имя куратора (`ls_curname`), имя поддержки (`ls_supname`), чекбокс подтверждения ASH (`ls_ash`).

## 2.25. Поставщик - `v326SupplierForm` / `v326SaveSupplier`

- **Открытие:** модалка `.v326-modal` (`v326:290`). **Проверка:** только `name` через `fieldWarn` (`v326:291`).
- Поля: Название (обяз.), Категория (`sp_cat`, select из `cfg.supplier.categories`), Ставка комиссии (`sp_rate`, number, значение по умолчанию из конфига), Скидка клиенту (`sp_disc`, number), Phone (`sp_phone`), Email (`sp_email`, **type=text**), Contact (`sp_contact`), Terms / notes (textarea).

## 2.26. Сделка с поставщиком - `v326SupplierDealForm` / `v326SaveSupplierDeal`

- **Открытие:** модалка `.v326-modal` (`v326:292`), с живым предпросмотром `v326SupplierPreview` (`v326:294`), который дополнительно выводит предупреждение о нераскрытии.
- **Проверка** (`v326:295`): поставщик, клиент, и при статусе `received` с включённым `requireDisclosureBeforePaid` - обе галочки раскрытия. Все три - через `fieldWarn`.
- Поля: Поставщик (`sd_sup`, select, обяз.), Клиент (`sd_client`, обяз.), Валюта (`sd_cur`, USD/UZS), Сумма заказа (`sd_order`, number), Ставка комиссии (`sd_rate`, number; при пустом подставляется ставка поставщика - `v326:293`), Скидка клиенту (`sd_disc`, number), Статус заказа (`sd_status`, select: expected / received / reversed), Клиент знает (`sd_client_disc`, checkbox), Поставщик знает (`sd_supplier_disc`, checkbox), Дата раскрытия (`sd_discdate`, date, сегодня), Агент (`sd_owner`, text), Этап (`sd_stage`, **text со значением `order`, а не select**), Notes.

## 2.27. Настройки поставщиков - `v326SupplierSettings` / `v326SaveSupplierSettings`

Модалка (`v326:296`) с таблицей построчного редактирования категорий: на каждую категорию два инпута `ss_cat_cr_N` и `ss_cat_disc_N`. Плюс `ss_rate`, `ss_disc`, чекбокс `ss_req`. **Проверок нет** (`v326:297`).

---

# ЧАСТЬ 3. СРАВНЕНИЕ ОДИНАКОВЫХ ШАГОВ

## 3.1. Заведение клиента

| | Advisory | Аренда / продажа |
|---|---|---|
| Сущность | одна: `CASE_CLIENTS` | **шесть**: `BRANDS` (`core:2945` и `v35:410` - две формы на одну сущность), `BRAND_REQUESTS` (`v32:467`), `INVESTOR_REQUESTS` (`v32:473`), `SALES_BUYERS` (`v32:481`), `CASE_PARTNERS` (`v32:485`), `SUPPLIERS` (`v326:290`) |
| Форм на сущность | 1 (`v490:95`) | 7 форм на 6 сущностей |
| Открытие | модалка | выдвижная панель (`core`, `v35`) и модалка (`v32`, `v326`) |
| Число полей | 9 | 35+ (`v35:410`), 30+ (`core:2945`), 13 (`v32:467`), 18 (`v32:473`), 12 (`v32:481`), 11 (`v32:485`), 7 (`v326:290`) |
| Обязательные | название | название (везде по одному полю); в `core:2982` - **вообще ни одного**, пустое имя молча оставляет прежнее |
| Черновик | нет | есть, но только для нового бренда в `v35:365`; для документов - `v32:515` |
| Уведомление после сохранения | `toast('Сохранено')` (`v490:45`) | `v35:446` - `toast('Бренд сохранён')`; `v32` и `v326` - **тоста нет**; `core:2982` - нет |

Расхождения в наборе полей клиента (Advisory `v490:95` против «клиента» аренды):

| Поле | Advisory | Аренда |
|---|---|---|
| Юридическое наименование | есть (`c49_client_legal`) | **нет ни в одной форме** |
| Тип контрагента | select из 5 (`c49_client_type`) | нет; роль задаётся тем, в какой из шести сущностей заведена запись |
| Статус жизненного цикла | select Активный / Потенциальный / Архив | у бренда - `BRANDST` (другой словарь); у запроса/инвестора/актива - стадия воронки; у партнёра - pending/active/paused; у покупателя - жёстко `active` |
| Ответственный | `c49_client_owner`, свободный текст | у бренда - `bd_owner` с datalist из `USERS`+`AGENTS` (`v35:363`); у запроса/инвестора/актива - `broker` |
| Страна / город | **нет** | есть у запроса, инвестора, актива, покупателя, партнёра; у бренда только страна |
| Категория / подкатегория / тип | **нет** | есть у бренда (3 поля с datalist) и у запроса (`rq_cat`) |
| Требования к площади | **нет** | `amin`/`amax` у бренда, `areaMin`/`areaMax` у запроса, инвестора, покупателя |
| Бюджет | **нет** | `budgetMax` у запроса, `budgetMin`/`budgetMax` у инвестора и покупателя |
| Контакты | 3 отдельных поля: контактное лицо, телефон, email | v35 - массив строк (ФИО, должность, код страны, телефон, email) с добавлением/удалением; core - одна textarea с разделителем `|`; v32 - одно поле `contact` (у покупателя подпись «Email / Телефон» на одно поле) |
| Профиль для презентаций | **нет** | 10+ полей у бренда: год основания, ICSC, сеть точек/стран, оператор, о бренде, позиционирование, концепция, рекомендация |
| Изображения | **нет** | 3 блока у бренда в `core:2951` |
| Проверка дубля | **нет** | `core:2990` - `confirm` с предложением слияния `brandMerge`; `v32:477` - проверка по названию в нижнем регистре при конвертации инвестора в покупателя |

## 3.2. Создание сделки

| | Advisory | Аренда / продажа |
|---|---|---|
| Сущность | одна: `CASE_OPPORTUNITIES` (`v490:117`) | пять: помещение `U` со статусом (`core:3739`), `BRAND_REQUESTS` (`v32:467`), `INVESTOR_REQUESTS` (`v32:473`), `SALES_ASSETS` (`v32:479`), `LEASE_COMMISSION_DEALS` (`v326:232`) |
| Наборов стадий | **1** - 12 стадий `PIPE_STAGES` (`v490:9-14`) | **5 разных**: 7 статусов помещения (`core:3741`); 5 стадий запроса (`v32:467`); 8 стадий инвестора (`v32:473`); 7 стадий актива (`v32:479`); 8 стадий комиссионной сделки (`v326:195`) + отдельный статус оплаты из 4 значений |
| Вероятность | поле `c49_opp_probability` (0-100), участвует в weighted pipeline (`v490:37`, `104`) | поля нет ни в одной форме; вероятность вшита в код `leaseProb` (`v326:166`) и `commStageProb` (`core:453`) |
| Валюта | USD / UZS / EUR | `v326` - USD / UZS; `v32:480` - **жёстко USD, поля нет**; помещение - валюта объекта |
| Следующий шаг + дата | есть (`c49_opp_next`, `c49_opp_next_date`) | есть в `v32` (`rq_next`/`rq_due`, `ir_next`/`ir_due`, `sa_next`/`sa_due`); **нет** в `v326:232` и в форме помещения `core:3739` (там вместо этого отдельная модалка контрольных дат `core:3691`) |
| Ожидаемая сумма | `c49_opp_value` | `rq_budget`/`ir_bmin`/`sa_price`/`lc_C`; у помещения - производная `total = area × rate` |
| Kanban с перетаскиванием | есть (`v490:111,114,115`), сохранение сразу | есть `renderKanban`/`kanDrop` (`core:1925,1929`) |
| Обязательные поля при сохранении | клиент + название проекта (`v490:118`) | по одному текстовому полю в каждой форме; в `regEditRow`/`saveCellLive` (`core:1634`) - ни одного |
| Автодействия при смене стадии | подстановка следующего шага при `proposal`/`contract` (`v490:115`) | `commTrack` при смене брокера/статуса (`core:1631`, `1634`, `3767`), `commAutoLeaseDeal` (`core:465`) |

## 3.3. Подготовка коммерческого предложения

| | Advisory | Аренда |
|---|---|---|
| Что создаётся | запись `CASE_PROPOSALS` + набор `CASE_SCOPE_ITEMS` (`v492:67`) | запись `DOCREG` (`core:3850`) + `u.offer`/`u.offers` на помещениях + смена статуса помещения на «Предложено» + перезапись ставки в реестре (`core:4062+`) |
| Где форма | **две разные**: модалка `v490:123` (10 полей) и страница-конструктор `v492:52-60` (~25 полей + 31 услуга + 3 пакета) | страница `#dform`, вкладка КП (`core:3987-4010`), ~40 полей в 12 секциях |
| Какая открывается по кнопке «Создать КП» | конструктор v492 - `v492:78` подменяет `window.case49ProposalCreate`; модалка v490 доступна только через «Быстрое ручное КП» (`v492:60,71`) | одна форма |
| Модель цены | 5 моделей: за м², фикс, гибрид, по этапам, ручная (`v492:57`) | ставка $/м² + 7 типов корректировки от бюджетной ставки владельца (`core:3996`) |
| Обоснование отклонения цены | `c492_override_reason`, обязательно при ручной цене (`v492:67`) | **нет поля**; вместо этого при ручной правке ставки в реестре - `confirm` `rateEditGuard` (`core:1629`) |
| График платежей | 3 транша в процентах, сумма проверяется мягким `confirm` (`v492:58`, `67`) | предоплата и депозит в месяцах (`f_prep`, `f_dep`), проверок нет |
| Срок действия | «Действует до» - дата; **сегодня+14 в `v490:123`, сегодня+30 в `v492:58`** | «Срок действия КП, дней» - число, по умолчанию `10` (`core:3991`), пересчитывается в `validUntil` предложения |
| Состав работ | матрица из 31 услуги + 3 пакета (`v492:14`, `50`, `51`) или ручные строки Scope (`v490:132`) | помещения (multiple select), деятельность из `ACTS`, модель аренды из `RENTMODELS`, 5 видов коммуналок с методом оплаты |
| Версионирование | отдельные записи с полем `version`, новая версия - `case49ProposalClone` (`v490:131`) **без подтверждения** | `versions[]` внутри одной записи `DOCREG` (`core:3851`); работает только если номер документа задан - LOI с `no:'-'` не версионируется (`core:4118`) |
| Черновик формы | **нет** | `localStorage` per тип документа (`v32:515-517`) |
| Обязательные поля | v490: **ни одного** (`v490:124`); v492: проект + хотя бы одна услуга + причина при ручной цене (`v492:67`) | арендатор (`core:4053`) + через модуль v32: помещение, площадь > 0, ставка > 0 (`v32:494`) |
| Показ ошибок | `alert` | подсветка поля + сообщение под полем + сводка со ссылками сверху + прокрутка к полю + чеклист готовности в % (`v32:489-503`) - **самый развитый механизм в обеих воронках** |
| Выгрузка документа | предпросмотр в новом окне и `.doc` через Blob (`v492:69-70`) | `printDoc`, `dlWord`, отправка в WhatsApp / Email / Telegram (`core:4129-4134`) |
| Проверка бренда по базе | нет | `ensureBrandInBase` (`core:4044`), в v32 заменён на подсказку с кнопкой добавления (`v32:506-511`) |
| Конфликт с чужим предложением | нет | `confirm` со списком помещений, предложенных другим агентом (`core:4062+`) |

## 3.4. Переход к договору

| | Advisory | Аренда |
|---|---|---|
| Сущность договора | `CASE_CONTRACTS` - отдельная запись | **отсутствует** |
| Как создаётся | `case49ContractFromProposal` (`v490:138`) - запись создаётся **до** формы, авто-номер `CTR-NNN`, версия `1.0`, стоимость и условия оплаты копируются из КП; повторный вызов открывает существующий договор | нет создания; статус помещения переводится в `cs` («Контракт на подписании») и `cd` («Контракт подписан») селектом - `core:1625`, `core:1696`, `core:3753`; в `v326:239` - стадия `contract`/`signed`; в `v32:479` - `spa`/`closed` |
| Форма | `case49ContractEdit` - 11 полей (`v490:139`) | формы договора нет; ближайшее - документ LOI (`core:4012`) с 5 полями, помеченный «Черновик. Юридическая проверка обязательна» (`core:3961`) |
| Номер и версия | `c49_ctr_no`, `c49_ctr_version` | нет |
| Даты | подписания, начала работ, окончания | «Дата подписания» есть только в комиссионной сделке (`lc_signed`, `v326:237`); в реестре - контрольные даты произвольного типа (`core:3691`) |
| Шаблон документа | `c49_ctr_template` из `CASE_DOCUMENT_TEMPLATES` | нет |
| График оплаты | `c49_ctr_payment` (textarea) | нет |
| Обязательные поля | **ни одного** (`v490:140`) - можно сохранить со статусом «Подписан» без даты подписания и с нулевой суммой | - |
| Что происходит дальше | возможности ставится `stage='won'`; `case49ContractLaunch` (`v490:164`) создаёт проект, копирует Scope со снимком baseline, генерирует задачи по шаблонам и deliverables - **без `confirm`** | смена статуса помещения → пересчёт `gap`, `commTrack` (`core:1631`); отдельно вручную заводится комиссионная сделка `v326:232` |
| Связь КП → договор | жёсткая: `c.proposalId`, снимок Scope, поиск существующего договора по `proposalId` (`v490:138`) | связи нет: комиссионная сделка `v326:232` хранит `objectName` и `unit` **строками**, не ссылками на `OBJECTS`/`U`, и никак не связана с выпущенным КП из `DOCREG` |

## 3.5. Расхождения в поведении, общие для всех форм

| Аспект | Advisory | Аренда |
|---|---|---|
| Контейнер формы | 1 модалка (`v490:77`) + 1 своя модалка (`v492:38`) + 1 страница (`v492:52`) | панель `#drawer` + модалка `#rmodal` + модалка `.v32-modal` + модалка `.v326-modal` + страница `#dform` + 7 вызовов `prompt()` |
| Хелперы разметки | `field`/`selectField`/`textField` (`v490:78-80`) - без placeholder и без `required` | `_bF`/`_bFL`/`_bTA`/`_bSel` (`core:2941-2944`), `formInput`/`formText`/`formSelect` (`v32:461-463`, без атрибутов вообще), `inp`/`area`/`sel` (`v326:198-200`, атрибуты поддерживаются, но в формах не используются), `input`/`select`/`area` (`v35:367-369`, с datalist) |
| Числовые ограничения | `min`/`max`/`step` расставлены почти везде (`v490:117,132,200,211`; `v492:57,58`) | почти нигде: `v32:461` не поддерживает атрибуты, `v326` их не передаёт, `core` использует `text`-инпуты для чисел. Исключения: `core:1695` (`step="0.1" min="0"`), `v32:408` (`min="0" step="1"`) |
| Подсказки в полях | 2 `placeholder` во всём `v490`, 5 в `v492` | 49 в `core`, 6 в `v35`, 3 в `v32`, 0 в `v326` |
| Уведомление об успехе | `toast('Сохранено')` в общем `save()` (`v490:45`) - единообразно во всех формах | `v490`-подобной централизации нет: `v492:19` тоста не даёт; `v32:193` не даёт; `v326:148` не даёт; `core` - точечно (`core:1721`); `v35:446` - даёт |
| Проверка прав в форме | `canEdit`/`canApprove`/`isAdmin` (`v490:39-41`) используются точечно: чекбокс согласования (`v490:178`, `230`), но сами формы редактирования правами не закрыты | `R().edit`, `R().admin`, `canManage()`, `caseCanEditRegistry()`, `rights().approve`, `canAll()` - проверяются на входе в `addObjectForm` (`core:1588`), `unitSave`, `deleteUnit`, `v32PartnerForm`, поля ASH (`v326:240`) |

---

# ЧАСТЬ 4. ПОТЕРЯ ДАННЫХ, ОТСУТСТВИЕ ОТМЕНЫ, ОТСУТСТВИЕ ПРОВЕРОК

## 4.1. Где формы теряют введённые данные при перерисовке

| Место | Файл:строка | Что происходит |
|---|---|---|
| Добавление контакта в форме документа | `core:3842` | цепочка из четырёх `prompt`, затем `pickT(DOC.t,DOC.uid)` - **полная пересборка `#dform`**. Все введённые значения КП сбрасываются на дефолты разметки |
| Восстановление черновика документа | `v32:516` | `restoreDocDraft` восстанавливает поле **только `if(!el.value)`**. Поля с непустыми дефолтами (`f_no='CA/0001'`, `f_valid=10`, `f_area`, `f_rate`, `f_cond`, `f_sig`) из черновика **не восстанавливаются никогда** - после пересборки формы в них остаются дефолты, а не то, что ввёл пользователь |
| Черновик карточки бренда | `v35:365` | `if(!dr||dr.dataset.brandDraft!=='new')return;` - при **редактировании существующего** бренда черновик не пишется; закрытие панели (`v35:448`) теряет правки |
| Закрытие модалки Advisory кликом по подложке | `v490:77` | `mousedown` по фону закрывает модалку немедленно, без предупреждения. Касается всех 15 форм Advisory на этом механизме |
| То же в `#rmodal` и `.v32-modal` | `core:3320`, `v32:176` | клик по фону закрывает форму отказа, контрольной даты, переноса варианта в отказ, запроса, инвестора, актива, покупателя, партнёра |
| Выбор пакета в конструкторе КП | `v492:61` | `case492SelectPackage` перезаписывает `c492_rate` и `c492_days` **введённые вручную**, без подтверждения; заодно сбрасывает все галочки услуг на набор пакета |
| Смена типа проекта в конструкторе КП | `v492:62` | `case492TypeChange` перезаписывает модель цены `c492_price_mode` и ставку `c492_rate` |
| Смена возможности в конструкторе КП | `v492:63` | `case492OppChange` перезаписывает поля «Клиент» и «Проект» без подтверждения |
| Снятие всех галочек услуг | `v492:43`, `52` | `serviceKeys()`: `if(checked.length||UI.customMode)keys=checked;` - при нуле отмеченных и выключенном ручном режиме возвращается набор пакета. `renderProposalBuilder` при `!UI.selectedServices.length` восстанавливает исходный набор из редактируемого КП. Итог: **«снять все услуги» не сохраняется** |
| Конструктор КП как страница | `v492:52-60`, `74` | форма живёт в DOM страницы; любой `go()` (навигация, фильтр, любое действие другого модуля) вызывает `main.innerHTML=…` и уничтожает несохранённые поля. В `UI` переживают перерисовку только `pendingCustom`, `selectedServices`, `packageId`, `projectType`, `pricingMode`, `customMode` - остальные ~15 полей (номер, клиент, проект, локация, понимание, площадь, ставка, фикс, сроки, версия, дата, транши, статус, примечания) хранятся только в DOM |
| Чекбокс выбора задачи | `v490:192` | `case49TaskSelect` вызывает `go(S.view)` - полная перерисовка таблицы на каждый клик |
| Поиск по клиентам | `v490:94` | `go('crm_clients')` через 160 мс после ввода - перерисовка всей страницы, а не только результатов. Ср. `v35:337 renderBrandsResultsOnly` и `v35:226-242 focusInfo/restoreFocusInfo`, где в аренде эта проблема решена восстановлением фокуса и позиции курсора |
| Инлайн-статус задачи | `v490:194` | `case49TaskQuick` → `save()` + `go(S.view)` |
| Смена стадии возможности перетаскиванием | `v490:115` | `save()` + `go('advisory_pipeline')` |
| Кнопка «Создать КП» внутри формы возможности | `v490:117` | уходит в другую форму, **не сохранив** изменения текущей |

Единственное место в обеих воронках с явной защитой поля от перерисовки - комментарий в реестре аренды: черновик `REG_CDRAFT` + восстановление фокуса и позиции курсора (`core:1709`, `1720-1733`), проблема описана в комментарии `core:1712-1719`.

## 4.2. Где нет отмены

**Мгновенное сохранение без возможности откатить:**

| Действие | Файл:строка |
|---|---|
| Смена статуса задачи селектом в таблице | `v490:187`, `194` |
| Перетаскивание карточки задачи в Kanban | `v490:199` |
| Перетаскивание возможности между стадиями | `v490:115` |
| Смена исполнителя селектом - визуально применяется до подтверждения, отмена возвращает через перерисовку | `v490:187`, `196` |
| Смена статуса продажи в SCR | `v32:407`, `452` |
| Изменение цены продажи в SCR | `v32:408`, `453` (`audit` не пишется) |
| Все поля строки в режиме правки реестра | `core:1694`, `1634` (`u.hist` и `audit` не пишутся) |
| Селект статуса и брокера в `editCell` | `core:1625-1626`, `1631` |
| Статус выпущенного документа | `core:3886` |
| Добавление в шорт-лист из подбора | `v32:471`, `476`; `core:3805` |

**Деструктивные действия без `confirm`:**

| Действие | Файл:строка |
|---|---|
| «Новая версия» КП - создаёт запись и копирует весь Scope | `v490:131` |
| Архивирование версии планировки | `v490:225` (есть только `alert`, если версия действующая) |
| Запуск проекта из договора - создаёт объект, задачи и deliverables | `v490:164` |
| Массовое переназначение исполнителя | `v490:203` |
| Массовая смена статуса задач | `v490:205` |
| «Очистить» в конструкторе КП - сбрасывает всю форму | `v492:60`, `68` |
| Удаление кастомной работы в конструкторе КП | `v492:65` |
| «Очистить черновик» в карточке бренда | `v35:428` (инлайн `localStorage.removeItem`) |
| Удаление строки контакта в карточке бренда | `v35:407` |
| `doMerge` - объединение помещений с удалением одного из реестра | `core:3645` (`confirm` есть только у `deleteUnit` `core:1623` и `deleteObject` `core:1613`) |
| `v32InvestorToBuyer` - создание покупателя из инвестор-лида | `v32:477` |

**Формы, где кнопка «Отмена» есть, но она не предупреждает о несохранённых изменениях:** все модалки `v490` (`v490:95,117,123,129,132,139,179,196,200,202,204,211,221,237`), `v326:244,265,290,292,296`, `v32:467,473,479,481,485`, `core:1608,3758,2978,3643,3700,3774`.

**Формы, где кнопки «Отмена» нет вообще:**
- страница документов `#dform` (`core:3988-4015`) - только уход навигацией;
- конструктор КП `v492:60` - вместо отмены «Очистить», которая стирает всё.

## 4.3. Где нет проверки обязательных полей

**Формы без единой проверки:**

| Форма | Файл:строка сохранения |
|---|---|
| Создание КП (модалка Advisory) | `v490:124` - ни номера, ни суммы, ни связи с воронкой |
| Изменение КП | `v490:130` |
| **Договор Advisory** | `v490:140` - можно сохранить со статусом «Подписан», без даты подписания, с нулевой суммой |
| Массовое переназначение | `v490:203` - можно назначить пустого исполнителя |
| Массовая смена статуса | `v490:205` |
| Карточка бренда в core | `core:2982` - `b.name=g('e_name')||b.name`, пустое название молча оставляет прежнее |
| Инлайн-правка строки реестра | `core:1634` - нет даже `rateEditGuard`, который есть в `saveCell` (`core:1631`) |
| Настройки комиссии по аренде | `v326:267` - суммы долей не сверяются со 100% |
| Настройки поставщиков | `v326:297` |
| Объединение помещений | `core:3645` |
| Перенос варианта в отказной лист | `v490` - н/д; аренда: `core:3685` - причина отказа не обязательна |

**Формы с проверкой ровно одного поля** (при том что запись без остальных бесполезна):

| Форма | Проверяется | Файл:строка |
|---|---|---|
| Клиент Advisory | название | `v490:96` |
| Работа Scope | название | `v490:133` |
| Задача | название | `v490:201` |
| Deliverable | название | `v490:212` |
| Шаблон документа | название | `v490:238` |
| Запрос бренда | бренд | `v32:468` |
| Инвестор-лид | инвестор | `v32:474` |
| Актив на продажу | название | `v32:480` |
| Покупатель | название | `v32:482` |
| Партнёр | название | `v32:486` |
| Поставщик | название | `v326:291` |
| Бренд (v35) | название | `v35:446` |
| Объект | название | `core:1610` |

**Проверки, которые есть только благодаря другому модулю:** обязательность помещения, площади и ставки в КП и LOI живёт не в `core:4050`, а в обёртке `v32:504-505` над `genKP`/`genLOI`/`genREM`. Если `v32-upgrade.js` не загружен, `genKP` проверит **только арендатора** (`core:4053-4054`).

**Проверки, которые не блокируют:**
- график платежей ≠ 100% в конструкторе КП - `confirm`, а не отказ (`v492:67`);
- ручная правка ставки при активном КП - `confirm` (`core:1629`);
- дубль бренда по названию - `confirm` (`core:2990`);
- КП поверх чужого предложения - `confirm` (`core:4062+`).

**Молчаливый отказ без сообщения:**
- `case492AddCustomService` (`v492:65`) при пустом названии - `if(!title)return;`, пользователю ничего не показывается;
- `addVar` (`core:3673`) - `if(v)`, пустая строка проглатывается.

**Как показывается ошибка - пять разных механизмов:**

| Механизм | Где | Что делает |
|---|---|---|
| `alert()` | `v490` (все формы), `v492:39,67`, `core:1610,2788,3707,3764` | системное окно, поле не подсвечивается, фокус не переводится |
| `toast(msg,'warn')` | `v32:468,474,480,482,486` | всплывающее сообщение, поле не подсвечивается |
| `fieldWarn` | `v326:260` | класс `v326-invalid` + `focus()` + `alert()` |
| фокус + подсветка + тост | `v35:446` | `focus()` + `borderColor='var(--red)'` + `toast` |
| подсветка + сообщение под полем + сводка + прокрутка + чеклист % | `v32:489-503` (только форма документов) | `case-invalid`, `.case-field-error`, `#v32DocErrors` со ссылками `v32JumpField`, живой `.v32-checklist` |
