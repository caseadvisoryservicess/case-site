# CASE OS v4.50.1 — краткая установка

Полная инструкция находится в корне пакета: `CASE_OS_v4.50.1_DEPLOY_RU.md`.

1. Замените всё содержимое папки `/os` содержимым архива `CASE_OS_v4.50.1_os_only.zip`.
2. Ctrl+Shift+R. В шапке `v4.50.1`, предупреждения о старых файлах нет.

Изменены `index.html`, `v4327-patch.js`, `sw.js`. Ядро `core.js` не менялось —
останется в кэше браузера. Серверные PHP не менялись, миграций базы нет.
