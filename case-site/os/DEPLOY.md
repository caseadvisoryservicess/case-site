# CASE OS v4.43.1 — краткая установка

Полная инструкция находится в корне пакета: `CASE_OS_v4.43.1_DEPLOY_RU.md`.
После загрузки выполните hard reload.

1. Замените содержимое папки `/os` содержимым архива `CASE_OS_v4.43.1_os_only.zip`.
2. Проверьте: в шапке `v4.43.1`, в `sw.js` cache key `case-os-v4431`.
3. Если не делали после v4.42.1 — один вход администратором (миграция шаблонов ролей).
