# CASE OS v4.35.0 — краткая установка

Полная инструкция находится в корне пакета: `CASE_OS_v4.35.0_DEPLOY_RU.md`.
Новых SQL-миграций нет; обновлены серверные файлы в `os/api/` (config.php не затрагивается).
После загрузки выполните hard reload.

1. Замените содержимое папки `/os` содержимым архива `CASE_OS_v4.35.0_os_only.zip`.
2. Проверьте: в шапке `v4.35.0`, в `sw.js` cache key `case-os-v4350`.
3. `os/api/geo_collector.php` из браузера должен отвечать `403 CLI only`.
