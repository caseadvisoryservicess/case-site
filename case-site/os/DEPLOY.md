# CASE OS v4.42.2 — краткая установка

Полная инструкция находится в корне пакета: `CASE_OS_v4.42.2_DEPLOY_RU.md`.
SQL-миграций нет; серверные файлы не менялись. После загрузки выполните hard reload.

1. Замените содержимое папки `/os` содержимым архива `CASE_OS_v4.42.2_os_only.zip`.
2. Проверьте: в шапке `v4.42.2`, в `sw.js` cache key `case-os-v4422`.
3. Если не делали после v4.42.1 — один вход администратором (миграция шаблонов ролей).
