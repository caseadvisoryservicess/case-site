# CASE OS v4.50.2 — краткая установка

Полная инструкция в корне пакета: `CASE_OS_v4.50.2_DEPLOY_RU.md`.

1. Замените всё содержимое папки `/os` содержимым архива `CASE_OS_v4.50.2_os_only.zip`.
2. Ctrl+Shift+R. В шапке `v4.50.2`.

Изменены `index.html`, `v4327-patch.js`, `quiz-data.js`, `sw.js`.
Ядро `core.js` не менялось — останется в кэше. Серверные PHP не менялись, миграций базы нет.
